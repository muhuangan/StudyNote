---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第十章 脉冲波形的产生和整形 / 10.4 多谐振荡器

# 多谐振荡器

## 核心结论

多谐振荡器**没有稳态**, 只有两个可以自动交替的暂稳态, 因此无需外界触发就能自激产生矩形波. 振荡频率由**电容在两个阈值之间充放电的时间**决定.

## 一、三种振荡器的对比

| 类型 | 稳态个数 | 是否需要触发 | 输出 |
| ---- | -------- | ------------ | ---- |
| 双稳态(触发器) | 2 个稳态 | 需要 | 保持状态 |
| 单稳态 | 1 稳态 + 1 暂稳态 | 需要 | 一个宽度为 $t_w$ 的脉冲 |
| **多谐振荡器(无稳态)** | 0 个稳态 | 不需要 | 连续的矩形波 |

**自激振荡的条件**: 电路处于任何一个暂稳态时, 都会通过 RC 充放电自动走向另一个暂稳态, 如此往复.

## 二、由施密特触发器构成

**结构**: 施密特反相器的输出经电阻 $R$ 反馈到输入端, 输入端对地接电容 $C$.

**工作过程**:

1. 输出为高电平时, 经 $R$ 对 $C$ 充电,$v_C$ 上升.  
2. $v_C$ 达到上限阈值 $V_{T+}$ 时, 输出翻转为低电平.  
3. $C$ 经 $R$ 放电,$v_C$ 下降.  
4. $v_C$ 降到下限阈值 $V_{T-}$ 时, 输出翻转为高电平, 回到第 1 步.  

**周期计算**(CMOS 电源 $V_{DD}$):

充电时间(从 $V_{T-}$ 到 $V_{T+}$, 终值 $V_{DD}$):

$$t_1 = RC \ln\frac{V_{DD} - V_{T-}}{V_{DD} - V_{T+}}$$

放电时间(从 $V_{T+}$ 到 $V_{T-}$, 终值 0):

$$t_2 = RC \ln\frac{V_{T+}}{V_{T-}}$$

$$T = t_1 + t_2,\quad f = \frac{1}{T}$$

**优点**: 只需一个施密特触发器加一个 R, 一个 C, 结构最简单.

## 三、由 555 定时器构成(最常用)

**接法**:

- $R_1$ 接在 $V_{CC}$ 与 7 脚(DIS) 之间.  
- $R_2$ 接在 7 脚与 2,6 脚之间.  
- $C$ 接在 2,6 脚与地之间.  
- 5 脚经 0.01 微法电容接地.  

**工作过程**:

| 阶段 | 电容路径 | 电容电压 | 输出 |
| ---- | -------- | -------- | ---- |
| 充电 | 经 $R_1 + R_2$ | 从 $\frac{1}{3}V_{CC}$ 升到 $\frac{2}{3}V_{CC}$ | 高电平 |
| 放电 | 经 $R_2$(放电管导通) | 从 $\frac{2}{3}V_{CC}$ 降到 $\frac{1}{3}V_{CC}$ | 低电平 |

**参数计算**:

$$t_1 = (R_1 + R_2) C \ln 2 \approx 0.69 (R_1 + R_2) C$$

$$t_2 = R_2 C \ln 2 \approx 0.69 R_2 C$$

$$T = t_1 + t_2 \approx 0.69 (R_1 + 2R_2) C$$

$$f = \frac{1}{T} \approx \frac{1.43}{(R_1 + 2R_2) C}$$

**占空比**:

$$q = \frac{t_1}{T} = \frac{R_1 + R_2}{R_1 + 2R_2}$$

**重要结论**: 这种接法的占空比**恒大于 50%**, 且无论如何调节 $R_1$,$R_2$ 都无法低于 50%(因为充电必然经过 $R_1 + R_2$, 放电只经过 $R_2$, 充电时间一定更长).

**占空比可调的改进接法**: 在 $R_2$ 上并联一个二极管(阳极接 7 脚, 阴极接 2/6 脚), 使充电只经 $R_1$, 放电只经 $R_2$:

$$t_1 \approx 0.69 R_1 C,\quad t_2 \approx 0.69 R_2 C$$

$$T \approx 0.69 (R_1 + R_2) C,\quad q = \frac{R_1}{R_1 + R_2}$$

此时取 $R_1 = R_2$ 即可得到 **50% 占空比**.

## 四、由门电路构成(对称式多谐振荡器)

**结构**: 两个反相器(或非门) 交叉耦合, 各自通过电容耦合到对方的输入端, 并接有偏置电阻.

**振荡频率**(对称参数 $R$,$C$, 阈值 $V_{TH}$):

$$T \approx 2 \cdot RC \ln\frac{V_{DD}}{V_{TH}} \approx 1.4 RC$$

(CMOS 门 $V_{TH} \approx V_{DD}/2$ 时)

**特点**: 结构对称, 波形对称, 但对元件参数一致性要求高.

## 五、石英晶体振荡器

**为什么需要晶体**: RC 振荡器的频率由 $R$,$C$ 决定, 而电阻电容的误差(±5% 至 ±20%) 与温度漂移很大, 无法满足精确定时要求.

**晶体的特性**: 石英晶体具有**压电效应**, 其等效电路是一个高 $Q$ 值的谐振回路, 谐振频率由晶体的机械尺寸决定, 极其稳定(温度系数可达百万分之几).

**两种谐振模式**:

| 模式 | 频率 | 等效 |
| ---- | ---- | ---- |
| 串联谐振 $f_s$ | 较低 | 阻抗最小 |
| 并联谐振 $f_p$ | 较高 | 阻抗最大 |

**典型电路**:

1. **并联型晶体振荡器**: 晶体工作在 $f_s$ 与 $f_p$ 之间, 等效为电感, 与外部电容构成电容三点式振荡器(Pierce 振荡器). 单片机与 FPGA 的片内振荡器多采用此结构.  
2. **串联型晶体振荡器**: 晶体工作在 $f_s$, 等效为短路, 构成正反馈通路.  

**使用要点**:

1. 晶体两端必须按手册要求接**负载电容**(通常 10 至 30 pF), 否则频率会偏离标称值.  
2. 晶体要尽量靠近振荡引脚, 走线要短, 要包地.  
3. 无源晶体需要芯片内部的振荡器配合;**有源晶振**内部已含振荡电路, 直接输出方波, 使用方便但成本高.  

## 六、环形振荡器

**结构**: 奇数个反相器首尾相接构成环.

**原理**: 奇数级反相使环路增益为负, 加上每级的传输延迟, 形成自激振荡.

$$f = \frac{1}{2 n \cdot t_{pd}}$$

其中 $n$ 是反相器级数,$t_{pd}$ 是每级延迟.

**特点**: 频率极高但极不稳定(随温度, 电压, 工艺大幅变化), 一般只用于:

1. 芯片内部的**工艺/电压/温度监测**(PVT 传感器).  
2. 真随机数发生器的熵源.  
3. 片上时钟源(需要锁相环稳定).  

## 七、振荡器的选型

| 需求 | 选择 |
| ---- | ---- |
| 精度要求低, 成本敏感 | 555 或 门电路 RC 振荡器 |
| 需要可调占空比 | 555 + 二极管改进接法 |
| 需要精确频率(串口, 计时) | **晶体振荡器** |
| 需要宽范围可调 | 压控振荡器(VCO)+ 锁相环(PLL) |
| 需要多种频率 | 晶体 + PLL 倍频/分频 |

## 八、设计与调试要点

1. **起振条件**: 环路增益大于 1, 相移为 360 度(正反馈).  
2. **稳定性**: RC 参数选择应避免频率过高(超过器件带宽) 或过低(漏电影响显著).  
3. **占空比要求**: 普通 555 接法无法低于 50%, 要注意.  
4. **输出驱动**: 555 输出电流可达 200 mA, 可直接驱动 LED, 继电器, 小喇叭.  
5. **去耦**: 电源端必须接 0.1 微法去耦电容, 否则输出波形会叠加电源干扰.  

## 易错点

- 认为 555 多谐振荡器能得到任意占空比: 标准接法占空比恒大于 50%, 要得到 50% 必须加二极管改进.  
- 认为 RC 振荡器频率精确: RC 元件的误差与温漂使频率精度只能达到百分之一量级, 精确定时必须用晶体.  

## 相关笔记

- 前置:[脉冲波形参数](./脉冲波形参数.md)  
- 器件:[555定时器](./555定时器.md)  
- 相关:[施密特触发器](./施密特触发器.md),[单稳态触发器](./单稳态触发器.md)  
- 分频:[计数器](./计数器.md)  
