---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第五章 触发器 / 5.5 触发器的逻辑功能描述方法

# 触发器逻辑功能的描述

## 核心结论

同一个触发器可以用**特性表, 特性方程, 状态转换图, 激励表, 波形图**五种方式描述, 它们完全等价. 分析和设计时序电路时, 选择最方便的一种: 分析用特性方程, 设计用激励表.

## 一、特性表(真值表)

以输出次态 $Q^*$ 为函数, 列出全部输入与现态组合下的次态. JK 触发器的特性表:

| J | K | Q | $Q^*$ | 说明 |
| - | - | - | ----- | ---- |
| 0 | 0 | 0 | 0 | 保持 |
| 0 | 0 | 1 | 1 | 保持 |
| 0 | 1 | 0 | 0 | 置 0 |
| 0 | 1 | 1 | 0 | 置 0 |
| 1 | 0 | 0 | 1 | 置 1 |
| 1 | 0 | 1 | 1 | 置 1 |
| 1 | 1 | 0 | 1 | 翻转 |
| 1 | 1 | 1 | 0 | 翻转 |

## 二、特性方程

由特性表经卡诺图化简得到次态的代数表达式.

| 类型 | 特性方程 | 约束 |
| ---- | -------- | ---- |
| SR | $Q^* = S + \overline{R}Q$ | $SR = 0$ |
| JK | $Q^* = J\overline{Q} + \overline{K}Q$ | 无 |
| D | $Q^* = D$ | 无 |
| T | $Q^* = T \oplus Q = T\overline{Q} + \overline{T}Q$ | 无 |

**用法**: 时序电路分析时, 把驱动方程(输入端的逻辑表达式) 代入特性方程, 即得状态方程.

## 三、状态转换图

用圆圈表示状态(0 或 1), 带箭头的弧线表示转换, 弧线上标注转换条件.

**JK 触发器的状态转换图规则**:

- 0 → 0:$J = 0$,$K = \times$  
- 0 → 1:$J = 1$,$K = \times$  
- 1 → 0:$J = \times$,$K = 1$  
- 1 → 1:$J = \times$,$K = 0$  

**读法**:"0 状态在 $J = 0$ 时保持, 在 $J = 1$ 时转到 1", 与 $K$ 无关(因为 $Q = 0$ 时 $J\overline{Q} = J$,$\overline{K}Q = 0$,$K$ 不起作用).

**优势**: 状态转换图最直观地反映"状态是如何流动的", 是时序电路设计中画状态图的原型.

## 四、激励表(驱动表)

**定义**: 激励表是特性表的逆查表—已知现态 $Q$ 与期望次态 $Q^*$, 反查需要施加什么样的输入(激励).

**用途**: 时序电路**设计**的核心工具. 设计的流程是"先确定状态转换关系 → 查激励表得到输入端应接什么 → 求出驱动方程".

**四种触发器的激励表**:

| $Q \to Q^*$ | SR(S, R) | JK(J, K) | D | T |
| ----------- | ---------- | ---------- | - | - |
| 0 → 0 | 0, $\times$ | 0, $\times$ | 0 | 0 |
| 0 → 1 | 1, 0 | 1, $\times$ | 1 | 1 |
| 1 → 0 | 0, 1 | $\times$, 1 | 0 | 1 |
| 1 → 1 | $\times$, 0 | $\times$, 0 | 1 | 0 |

**读法示例**: JK 触发器要从 0 变到 1, 需要 $J = 1$,$K$ 可任意(因为 $Q = 0$ 时 $K$ 不起作用).

**D 触发器最特殊**: 激励就是次态本身($D = Q^*$), 因此用 D 触发器设计时最简单, 直接把次态逻辑接到 $D$ 端即可. 这也是现代设计几乎清一色使用 D 触发器的原因.

## 五、波形图(时序图)

按时间轴画出时钟, 输入与输出的波形, 是最接近实际测量的描述方式.

**读图要点**:

1. 先确定触发方式(上升沿还是下降沿).  
2. 只在有效沿处观察输入值, 确定该沿之后的次态.  
3. 沿与沿之间输出保持不变.  
4. 注意异步置位/复位端的优先级高于时钟.  

## 六、五种方法的等价转换

| 从 | 到 | 方法 |
| -- | -- | ---- |
| 特性表 | 特性方程 | 卡诺图化简 |
| 特性表 | 状态转换图 | 按现态分组, 提取转换条件 |
| 特性表 | 激励表 | 行列互换(按 $Q \to Q^*$ 重新查表) |
| 特性方程 | 状态转换图 | 代入 $Q = 0$ 与 $Q = 1$ 分别分析 |
| 任意 | 波形图 | 沿时刻采样, 逐周期推进 |

## 七、设计中选择触发器的依据

| 触发器 | 激励复杂度 | 适用场合 |
| ------ | ---------- | -------- |
| D | 最简单($D = Q^*$) | **首选**, 尤其适合 FPGA/CPLD |
| JK | 中等 | 门电路搭建时可用较少门实现 |
| T | 只适合翻转/保持 | 计数器, 分频器 |
| SR | 有约束, 基本不用 | 仅作教学或异步置位复位 |

**重要实践结论**: 在 FPGA 中只有 D 触发器(LE 内集成的是 D 触发器), 所有 JK, T 的功能都由 D 触发器加组合逻辑实现.

## 易错点

- 激励表的"$\times$"表示任意, 求驱动方程时应把它当作**无关项**用于化简, 直接填 0 会丢失最简解的机会.  
- 特性方程中的 $Q$ 是现态,$Q^*$ 是次态; 写状态方程时千万不要把 $Q^*$ 写成 $Q^{n+1}$ 以外的记号后自己混淆.  

## 相关笔记

- 器件基础:[边沿触发触发器](./边沿触发触发器.md)  
- 设计中的直接应用:[时序逻辑电路的设计方法](./时序逻辑电路的设计方法.md)  
- 分析中的直接应用:[时序逻辑电路的分析方法](./时序逻辑电路的分析方法.md)  
