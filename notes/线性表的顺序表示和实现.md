---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 2. 线性表

# 线性表的顺序表示和实现(顺序表)

逻辑相邻, 物理也相邻  
线性表的第一个地址叫:**基地址**,**或首地址**

**线性表顺序存储结构占用一片连续的存储空间**

顺序表优点: 任一元素可以随机存取

线性表的顺序实现:

```C
#define MAX_SIZE 100
typedef int ElemType;
typedef struct{
    ElemType elem[MAX_SIZE]; //存储元素的数组
    int length; //存储已经存储了几个元素
}SqList;
```

顺序表的初始化

```C
int InitList_Sq(SqList* L){
    L->length = 0;
    return 1;
}
```

顺序表的插入:  
自然语言描述: 从后往前, 把每个数都往后移一位, 最后把要插入的数插进去, Length++

顺序表的删除:  
自然语言描述: 从前往后, 把每个数往前移一位, Length--

顺序表的查找:  
从前往后遍历, 依次比较, 返回位置

## 相关笔记

- 上一篇:[线性表的类型定义](./线性表的类型定义.md)
- 下一篇:[线性表的链式表示和实现(链表)](./线性表的链式表示和实现.md)
- 返回索引:[数据结构](./数据结构.md)
