---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第五章 触发器 / 5.2 SR锁存器

# SR锁存器

## 核心结论

SR 锁存器是最简单的存储单元, 由两个门**交叉耦合**构成. 它的四条规则是: 置 1, 置 0, 保持, 禁止(或非门版本为 $S = R = 1$ 禁止; 与非门版本为 $\overline{S} = \overline{R} = 0$ 禁止).

## 一、由或非门构成(输入高电平有效)

**结构**: 两个或非门交叉耦合,$S$(Set, 置 1) 与 $R$(Reset, 置 0) 为输入,$Q$ 与 $\overline{Q}$ 为输出.

| S   | R   | $Q^*$ | 说明                                    |
| --- | --- | ----- | --------------------------------------- |
| 0   | 0   | $Q$   | **保持**原状态                          |
| 0   | 1   | 0     | **置 0**(复位)                          |
| 1   | 0   | 1     | **置 1**(置位)                          |
| 1   | 1   | 不定  | **禁止**:$\overline{Q}$ 与 $Q$ 不再互补 |

**为什么 $S = R = 1$ 禁止**: 此时两个或非门输出都被强制为 0, 即 $Q = \overline{Q} = 0$, 破坏了互补约定; 当 $S$,$R$ 同时撤除(回到 0,0) 时, 两个门会竞争, 最终状态取决于器件参数的微小差异,**无法确定**.

## 二、由与非门构成(输入低电平有效)

**结构**: 两个与非门交叉耦合, 输入为 $\overline{S}$(置 1 端, 低有效) 与 $\overline{R}$(置 0 端, 低有效).

| $\overline{S}$ | $\overline{R}$ | $Q^*$ | 说明     |
| -------------- | -------------- | ----- | -------- |
| 0              | 0              | 不定  | **禁止** |
| 0              | 1              | 1     | **置 1** |
| 1              | 0              | 0     | **置 0** |
| 1              | 1              | $Q$   | **保持** |

**记忆技巧**: 与非门版本中, 输入为 1(无效电平) 时相当于该输入端"不存在", 因此 $\overline{S} = \overline{R} = 1$ 时保持; 哪一个输入为 0, 就执行对应的置位/复位动作.

## 三、特性方程

把次态表示成输入与现态的函数, 用卡诺图化简得

$$Q^* = S + \overline{R}Q$$

**约束条件**:

$$SR = 0$$

即 $S$ 与 $R$ 不能同时为 1. 违反约束时特性方程不再成立(因为不定状态无法用二值逻辑描述).

## 四、状态转换图

用圆圈表示状态, 箭头表示转换方向, 箭头上标注转换条件($S$,$R$ 的取值):

| 现态  | 次态 | 条件                                                |
| ----- | ---- | --------------------------------------------------- |
| 0 → 0 | 保持 | $S = 0$,$R = 0$ 或 $R = 1$(即 $S = 0$,$R = \times$) |
| 0 → 1 | 置 1 | $S = 1$,$R = 0$                                     |
| 1 → 0 | 置 0 | $S = 0$,$R = 1$                                     |
| 1 → 1 | 保持 | $S = 0$ 或 $S = 1$,$R = 0$(即 $S = \times$,$R = 0$) |

## 五、门控(同步) SR 锁存器

**结构**: 在基本 SR 锁存器前加两个与门(或与非门) 作为输入控制门, 由使能信号 $EN$(或 $CLK$) 控制.

$$S' = S \cdot EN,\quad R' = R \cdot EN$$

| $EN$ | 行为                                   |
| ---- | -------------------------------------- |
| 0    | 输入被封锁, 锁存器**保持**             |
| 1    | 输入被接收, 锁存器按 SR 规则动作(透明) |

**意义**: 引入使能端后, 多个锁存器可以由同一时钟控制, 从而构成同步时序电路.

## 六、SR 锁存器的固有缺陷

1. **存在禁止状态**: 输入组合受限, 使用时必须保证 $SR = 0$.
2. **电平敏感**: 在 $EN = 1$ 的整个期间输出跟随输入, 任何输入端的毛刺都会被记录.
3. **存在空翻**: 若 $EN$ 期间输入变化多次, 输出也会变化多次(见"电平触发触发器").

## 七、典型应用

1. **按键消抖**: 机械按键的抖动会使电平反复跳变. 用 SR 锁存器(或专用消抖电路) 可以把抖动期间的多次跳变"锁定"为一次有效动作.
2. **异步置位/复位端**: 几乎所有集成触发器都带有不受时钟控制的**异步置位端 $\overline{S_D}$ 与异步复位端 $\overline{R_D}$**, 其内部结构正是 SR 锁存器.
3. **构成更复杂的锁存器**: D 锁存器就是在 SR 锁存器的输入端加一个反相器得到的($S = D$,$R = \overline{D}$, 永不违反约束).

## 八、SR 锁存器 → D 锁存器的演进

令 $S = D$,$R = \overline{D}$, 则 $S$ 与 $R$ 永远互补, 约束条件自动满足. 代入特性方程:

$$Q^* = D + \overline{\overline{D}}Q = D + DQ = D$$

得到 $Q^* = D$, 即 D 锁存器.**D 锁存器的价值就在于彻底消除了 SR 的禁止状态**, 这是功能演进的关键一步.

## 易错点

- 与非门版本与或非门版本的"禁止组合"电平相反, 必须先看清楚是哪种门构成.
- $\overline{S} = \overline{R} = 0$(与非门版本) 时输出 $Q = \overline{Q} = 1$, 这不是"两个状态都成立", 而是破坏了互补约定, 属于非法状态.
- 特性方程 $Q^* = S + \overline{R}Q$ 只在满足 $SR = 0$ 时才有效.

## 相关笔记

- 前置:[触发器概述](./触发器概述.md)
- 加使能后的形态:[电平触发触发器](./电平触发触发器.md)
- 应用:[施密特触发器](./施密特触发器.md)(按键去抖)
