---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 3. 栈和队列 / 3.3 栈的典型应用--表达式求和 / 3.3.2 中缀表达式如何转换为后缀表达式

# 中缀表达式如何转换为后缀表达式

1. 初始有一个栈, 在栈里有一个优先级为0的元素
2. 接下来扫描中缀表达式
3. 如果是数字则直接输出
4. 如果是运算符, 判断优先级, 如果优先级大于栈顶元素, 压入栈中, 否则将栈顶元素出栈输出, 然后将当前运算符压入栈中
5. 如果是左括号, 左括号在栈外的优先级最高, 在栈中的优先级为0
6. 如果是右括号, 则持续出栈并输出, 直到栈顶为左括号, 然后将左括号也出栈(不输出)
7. 遇到'\0', 输出所有栈中的内容

典型应用

```C
#include <stdio.h>
#include <stdlib.h>

#define MAXSIZE 100
typedef int ElemType;

typedef struct{
    ElemType *data;
    int top;
}Stack;

typedef enum{
    LEFT_PARE, RIGHT_PARE,
    ADD, SUB, MUL, DIV, MOD,
    EOS, NUM //EOS为'\0', NUM为判断内容为数字
}contentType;

char expr[] = "x/(i-j)*y"; //中缀表达式

//初始化
Stack* initStack(){
    Stack *s = (Stack*)malloc(sizeof(Stack));
    s -> data = (ElemType*)malloc(sizeof(ElemType) * MAXSIZE);
    s -> top = -1;
    return s;
}

//判断栈是否为空
int isEmpty(Stack *s){
    if(s -> top == -1){
        printf("空的\n");
        return 1;
    }
    else{
        return 0;
    }
}

//压栈
int push(Stack *s, ElemType e){
    if(s -> top >= MAXSIZE - 1){
        printf("满了\n");
        return 0;
    }
    s -> top++;
    s -> data[s->top] = e;
    return 1;
}

//出栈
int pop(Stack *s, ElemType *e){
    if(s -> top == -1){
        printf("空的\n");
        return 0;
    }
    *e = s -> data[s -> top];
    s -> top--;
    return 1;
}

//获取栈顶元素
int getTop(Stack *s, ElemType *e){
    if(s -> top == -1){
        printf("空的\n");
        return 0;
    }
    *e = s -> data[s -> top];
    return 1;
}

contentType getToken(char *symbol, int *index){
    *symbol = expr[*index];
    *index = *index + 1;
    switch(*symbol){
        case '(':
            return LEFT_PARE;
        case ')':
            return RIGHT_PARE;
        case '+':
            return ADD;
        case '-':
            return SUB;
        case '*':
            return MUL;
        case '/':
            return DIV;
        case '%':
            return MOD;
        case '\0':
            return EOS;
        default:
            return NUM;
    }
}

int print_token(contentType token){
    switch(token){
        case ADD:
            printf("+");
            break;
        case SUB:
            printf("-");
            break;
        case MUL:
            printf("*");
            break;
        case DIV:
            printf("/");
            break;
        case MOD:
            printf("%%");
            break;
        default:
            return 0;
    }
    return 1;
}

void postfix(Stack *s){
    int in_stack[] = {0, 19, 12, 12, 13, 13, 13, 0};
    int out_stack[] = {20, 19, 12, 12, 13, 13, 13, 0};
    contentType token;
    int index = 0;
    push(s, EOS);
    char symbol;
    ElemType e;

    token = getToken(&symbol, &index);

    while(token != EOS){
        if(token == NUM){
            printf("%c", symbol);
        }
        else if(token == RIGHT_PARE){
            while(s -> data[s -> top] != LEFT_PARE){
                pop(s, &e);
                print_token(e);
            }
            pop(s, &s->data[s -> top]);
        }
        else{
            while(in_stack[s -> data[s -> top]] >= out_stack[token]){
                pop(s, &e);
                print_token(e);
            }
            push(s, token);
        }
        token = getToken(&symbol, &index);
    }

    while(s -> data[s -> top] != EOS){
        pop(s, &e);
        print_token(e);
    }
}

int main(){
    Stack *s = initStack();
    printf("%s\n", expr);
    postfix(s);
    return 0;
}
```

## 相关笔记

- 上一篇: [如何计算后缀表达式](./如何计算后缀表达式.md)
- 下一篇: [朴素匹配](./朴素匹配.md)
- 返回索引: [数据结构](./数据结构.md)
