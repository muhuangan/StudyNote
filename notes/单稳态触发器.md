---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第十章 脉冲波形的产生和整形 / 10.3 单稳态触发器

# 单稳态触发器

## 核心结论

单稳态触发器有**一个稳态与一个暂稳态**: 平时停在稳态, 被触发后进入暂稳态, 经过由 RC 决定的时间 $t_w$ 后**自动**返回稳态. 它的核心价值是把"触发的时刻"转换成"一个精确宽度的脉冲".

## 一、工作状态

| 状态   | 特点                                                          |
| ------ | ------------------------------------------------------------- |
| 稳态   | 无外界触发时长期保持的状态, 输出可以是 0 或 1(取决于电路形式) |
| 暂稳态 | 触发后进入的临时状态, 持续 $t_w$ 后自动返回稳态               |

**三个要点**:

1. 必须由**外界触发信号**才能进入暂稳态.
2. 暂稳态持续时间 $t_w$ 由**电路自身的 RC 参数**决定, 与触发脉冲的宽度无关.
3. 暂稳态结束**自动**返回稳态, 不需要外界干预.

## 二、脉宽计算(通用 RC 公式)

暂稳态的持续时间由电容从初始电压充电(或放电) 到阈值电压的时间决定:

$$t_w = RC \ln\frac{v_C(\infty) - v_C(0^+)}{v_C(\infty) - V_{TH}}$$

其中:

- $v_C(\infty)$ 是电容充放电的终值电压;
- $v_C(0^+)$ 是电容的初始电压;
- $V_{TH}$ 是比较阈值.

**示例**(电容从 0 充电到 $\frac{2}{3}V_{CC}$, 终值为 $V_{CC}$):

$$t_w = RC \ln\frac{V_{CC} - 0}{V_{CC} - \frac{2}{3}V_{CC}} = RC \ln 3 \approx 1.1 RC$$

这正是 555 单稳态电路的经典公式.

## 三、由门电路构成(微分型与积分型)

**微分型单稳态触发器**:

- 由两个门加一个 RC **微分**电路构成.
- 触发脉冲经微分产生窄的尖脉冲, 使电路翻转进入暂稳态.
- 电容充电使暂稳态维持, 达到阈值后返回稳态.
- 特点: 对输入脉冲宽度**敏感**, 适用于窄触发脉冲.

**积分型单稳态触发器**:

- 由 RC **积分**电路构成.
- 特点: 对输入脉冲宽度不敏感(有抗干扰能力), 但输出波形的边沿较差.

## 四、由 555 定时器构成

**接法**:

1. 6 脚(THR) 与 7 脚(DIS) 并联后接 RC 定时网络($R$ 到 $V_{CC}$,$C$ 到地).
2. 2 脚(TRI) 作为触发输入端(**低电平触发**).
3. 5 脚(VCO) 通常经 0.01 微法电容接地, 以稳定内部参考电压.
4. 3 脚为输出.

**工作过程**:

| 阶段                                            | 状态                                                           |
| ----------------------------------------------- | -------------------------------------------------------------- |
| 稳态                                            | 放电管导通, 电容被短路,$v_C \approx 0$, 输出为 0               |
| 触发(2 脚出现低于 $\frac{1}{3}V_{CC}$ 的负脉冲) | 触发器置位, 放电管截止, 输出变 1, 电容开始经 $R$ 充电          |
| 暂稳态                                          | 输出保持 1, 电容电压上升                                       |
| 电容充到 $\frac{2}{3}V_{CC}$                    | 上比较器动作, 触发器复位, 放电管导通, 电容迅速放电, 输出回到 0 |

**脉宽**:

$$t_w = RC \ln 3 \approx 1.1 RC$$

**重要约束**: 触发脉冲的宽度必须**小于**输出脉宽 $t_w$, 否则在暂稳态期间触发端仍为低电平, 电路行为异常(输出脉宽会被延长). 若触发信号较宽, 应先经微分电路整形为窄脉冲.

## 五、集成单稳态触发器

| 型号    | 特点                                       |
| ------- | ------------------------------------------ |
| 74LS121 | 单稳态, 可重复触发可选, 脉宽由外接 RC 决定 |
| 74LS123 | 双可重触发单稳态                           |
| 74LS221 | 双单稳态, 带施密特触发输入                 |

**可重触发(retriggerable) 的含义**: 在暂稳态期间若再次触发, 暂稳态时间**重新开始计时**. 不可重触发器件在暂稳态期间忽略新触发.

## 六、典型应用

### 1. 定时

产生一个宽度精确的定时脉冲, 用于控制某个动作的持续时间. 例如"按下按键后让 LED 亮 5 秒".

### 2. 延时

把触发信号的下降沿(或上升沿)**延迟** $t_w$ 后输出. 若需要精确延时, 常把输出再经一级单稳态整形.

### 3. 脉冲展宽

把窄脉冲(如毛刺, 窄触发信号) 展宽成宽度足够被后续电路可靠采样的脉冲.

$$窄脉冲 \xrightarrow{单稳态} 宽度可控的宽脉冲$$

### 4. 脉冲整形

把形状不规则的输入脉冲变成宽度与幅度都规整的输出脉冲. 与施密特触发器的区别是: 单稳态**还能改变脉宽**, 施密特只能改变边沿.

### 5. 噪声抑制

只有超过一定宽度(由输入微分电路决定) 的触发脉冲才能触发电路, 窄的噪声脉冲被滤除.

## 七、脉宽精度与稳定度

**影响因素**:

| 因素           | 影响                                                     |
| -------------- | -------------------------------------------------------- |
| 电阻精度       | 直接线性影响 $t_w$                                       |
| 电容精度与漏电 | 直接影响, 电解电容误差可达 ±20%                          |
| 温度漂移       | 电容温度系数较大                                         |
| 电源电压       | 影响内部比较器阈值(555 因采用电阻分压, 比值基本不受影响) |
| 器件离散性     | 内部比较器阈值有公差                                     |

**提高精度的措施**:

1. 用金属膜电阻与 CBB/瓷片电容(避免电解电容).
2. 需要高精度时, 用**晶体振荡器 + 计数器**代替 RC 定时.
3. 555 的优越性: 内部阈值来自电阻分压的**比值**, 因此对电源电压变化不敏感, 这是它优于普通门电路单稳态的地方.

## 八、与施密特触发器的配合

实际系统中常常这样组合:

$$不规则输入 \xrightarrow{施密特触发器(整形边沿)} 方波 \xrightarrow{单稳态(设定宽度)} 规整脉冲$$

先用施密特解决"边沿不陡, 有抖动"的问题, 再用单稳态解决"脉宽不对"的问题.

## 易错点

- 触发脉冲宽度大于输出脉宽: 555 单稳态会异常, 必须先把触发信号微分整形.
- 认为输出脉宽等于触发脉宽: 输出脉宽由 RC 决定, 与触发无关—这正是单稳态的价值所在.
- 用电解电容做定时电容: 漏电与误差大, 脉宽会严重偏离计算值.

## 相关笔记

- 前置:[脉冲波形参数](./脉冲波形参数.md)
- 用 555 实现:[555定时器](./555定时器.md)
- 另一种整形:[施密特触发器](./施密特触发器.md)
- 脉冲产生:[多谐振荡器](./多谐振荡器.md)
