---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 3. 栈和队列 / 3.1 栈的定义与实现

# 栈的顺序结构实现

**栈**是只在表尾进行插入或删除操作的线性表, 表尾称为栈顶, 表头称为栈底, 不含任何元素的栈叫空栈

栈具有**后进先出**的性质(Last In First Out, LIFO)

**定义**

```C
#define MAXSIZE 100
typedef int ElemType;

typedef struct{
    ElemType data[MAXSIZE];
    int top;
}Stack;
```

**初始化**

```C
void initStack(Stack *s){
    s -> top = -1;
}
```

**压栈**

```C
int push(Stack *s, ElemType e){
    if(s -> top >= MAXSIZE -1){
        printf("满了\n");
        return 0;
    }
    s -> top++;
    s -> data[s -> top] = e;
    return 1;
}
```

**出栈**

```C
ElemType pop(Stack *s, ElemType *e){
    if(s -> top == -1){
        printf("空的\n");
        return 0;
    }
    *e = s -> data[s -> top];
    s -> top--;
    return 1;
}
```

## 相关笔记

- 上一篇:[线性表的链式表示和实现(链表)](./线性表的链式表示和实现.md)
- 下一篇:[栈的链式结构实现](./栈的链式结构实现.md)
- 返回索引:[数据结构](./数据结构.md)
