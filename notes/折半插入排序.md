---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 8. 排序 / 8.1 简单的排序算法 / 8.1.3 折半插入排序

# 折半插入排序

**排序**: 按一组数据元素按照某种规则(通常是关键字的大小) 重新排列成有序的过程

**排序的三大指标**:

1. 时间复杂度
2. 空间复杂度
3. 排序稳定性(相等元素排序前后相对顺序是否保持不变)

由于插入排序时需要挨个比较每个元素, 这太浪费了, 我们可以采用折半查找的思路找到应该插入新数据的位置

```C
#include <stdio.h>

// 折半插入排序
void binaryInsertSort(int* data, int len){
    int low, high, mid;
    int key;

    // 从第二个元素开始遍历,将其插入到前面已经排好序的子序列中
    for(int i = 1; i < len; i++){
        key = data[i]; //记录当前待插入的值
        low = 0; // 设置当前有序序列的最小下标
        high = i - 1; // 设置当前有序序列的最大下标

        // 使用折半查找法找出key应该插入的位置(即low最终的位置)
        while(low <= high){
            mid = (low + high) / 2;
            if(data[mid] > key){
                high = mid - 1; //插入点在左半边
            }
            else{
                low = mid + 1; // 插入点在右半边
            }
        }

        // 从后往前依次将大于key的元素后移,为key腾出空间
        int j = i - 1;
        while(j >= low){
            data[j + 1] = data[j];
            j--;
        }

        // 插入key到正确的位置
        data[low] = key;
    }
}

int main(){
    int data[] = {47, 35, 60, 95, 77, 15, 28};
    int len = sizeof(data) / sizeof(data[0]);

    binaryInsertSort(data, len);

    for(int i = 0; i < len; i++){
        printf("%d ", data[i]);
    }
    return 0;
}
```

| 维度       | 描述                                              |
| ---------- | ------------------------------------------------- |
| 时间复杂度 | 最坏: $O(n^2)$ 最好: $O(n \log n)$ 平均: $O(n^2)$ |
| 空间复杂度 | $O(1)$                                            |
| 算法稳定性 | 稳定                                              |
| 适用场景   | 数据基本有序或对稳定性有要求时效果较好            |

## 相关笔记

- 上一篇: [插入排序](./插入排序.md)
- 下一篇: [简单选择排序](./简单选择排序.md)
- 返回索引: [数据结构](./数据结构.md)
