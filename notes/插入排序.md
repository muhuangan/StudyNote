---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 8. 排序 / 8.1 简单的排序算法

# 插入排序

**排序**: 按一组数据元素按照某种规则(通常是关键字的大小) 重新排列成有序的过程

**排序的三大指标**:

1. 时间复杂度
2. 空间复杂度
3. 排序稳定性(相等元素排序前后相对顺序是否保持不变)

遍历每个元素跟已经排好序的元素进行比较, 找到元素应在的位置, 插入进去(类似扑克牌抓牌)

```C
#include <stdio.h>

void insertSort(int* data, int len){
    int key; //用于暂存当前待插入的元素
    for(int i = 1; i < len; i++){ //从第二个元素开始逐个插入
        key = data[i]; // 将当前元素保存为key
        int j = i - 1; // 从已排序序列的最后一个元素开始向前比较

        // 向前查找插入位置,移动比key大的元素
        while(j >= 0 && data[j] > key){
            data[j + 1] = data[j]; // 将比key大的元素后移
            j--; // 向前继续比较
        }

        // 找到插入位置后,将key插入
        data[j + 1] = key;
    }
}

int main(){
    int data[] = {47, 35, 60, 95, 77};
    int len = sizeof(data) / sizeof(data[0]);

    insertSort(data, len);

    for(int i = 0; i < len; i++){
        printf("%d", data[i]);
    }
    return 0;
}
```

| 维度       | 描述                                                         |
| ---------- | ------------------------------------------------------------ |
| 时间复杂度 | 最坏: $O(n^2)$ 最好: $O(n)$ 平均: $O(n^2)$                   |
| 空间复杂度 | $O(1)$                                                       |
| 算法稳定性 | 稳定                                                         |
| 适用场景   | 适合数据规模小或部分有序的情况, 常用于教学或小规模数据的处理 |

## 相关笔记

- 上一篇:[冒泡排序](./冒泡排序.md)
- 下一篇:[折半插入排序](./折半插入排序.md)
- 返回索引:[数据结构](./数据结构.md)
