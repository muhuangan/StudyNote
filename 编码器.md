---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第四章 组合逻辑电路 / 4.4.1 编码器

# 编码器

## 核心结论

编码器把 $2^n$ 个输入状态变换成 $n$ 位二进制代码,**是译码器的逆运算**. 普通编码器要求任意时刻只有一个输入有效, 而**优先编码器**允许多个输入同时有效, 并按预设优先级对最高者编码, 是实际使用的器件.

## 一、普通编码器(以 8 线-3 线为例)

**约定**: 8 个输入 $I_0$ 至 $I_7$ 高电平有效, 任意时刻只有一个为 1; 输出 3 位二进制码 $Y_2 Y_1 Y_0$.

**真值表(节选)**:

| 有效输入 | $Y_2$ | $Y_1$ | $Y_0$ |
| -------- | ----- | ----- | ----- |
| $I_0$ | 0 | 0 | 0 |
| $I_1$ | 0 | 0 | 1 |
| $I_2$ | 0 | 1 | 0 |
| $I_3$ | 0 | 1 | 1 |
| $I_4$ | 1 | 0 | 0 |
| $I_5$ | 1 | 0 | 1 |
| $I_6$ | 1 | 1 | 0 |
| $I_7$ | 1 | 1 | 1 |

**逻辑表达式**(由"哪个输入为 1 就输出对应码"直接得出):

$$Y_2 = I_4 + I_5 + I_6 + I_7$$

$$Y_1 = I_2 + I_3 + I_6 + I_7$$

$$Y_0 = I_1 + I_3 + I_5 + I_7$$

**致命缺陷**: 若两个输入同时为 1(如 $I_3$ 与 $I_5$), 输出为 $111$, 即 $I_7$ 的编码, 结果完全错误. 此外, 全部输入为 0 时输出也是 $000$, 与 $I_0$ 有效无法区分.

## 二、优先编码器

**改进**: 规定输入的优先级(通常下标大者优先,$I_7$ 最高), 当多个输入同时有效时,**只对优先级最高的那个编码**, 其余忽略.

**以 4 线-2 线优先编码器为例**($I_3$ 优先级最高):

$$Y_1 = I_3 + I_2$$

$$Y_0 = I_3 + \overline{I_2} I_1$$

推导逻辑: 只有当 $I_3$ 无效时才考虑 $I_2$, 只有当 $I_3$,$I_2$ 都无效时才考虑 $I_1$.

## 三、74LS148:8 线-3 线优先编码器

**引脚与有效电平**:

| 信号 | 名称 | 有效电平 |
| ---- | ---- | -------- |
| $\overline{I_0}$ 至 $\overline{I_7}$ | 编码输入端 | **低电平有效**,$\overline{I_7}$ 优先级最高 |
| $\overline{A_2}$,$\overline{A_1}$,$\overline{A_0}$ | 编码输出端 | **低电平有效**(反码输出) |
| $\overline{EI}$ | 输入使能端 | 低电平有效,$\overline{EI} = 0$ 时允许编码 |
| $\overline{EO}$ | 输出使能端 | 低电平有效, 用于级联扩展 |
| $\overline{GS}$ | 优先编码标志 | 低电平有效, 表示"有有效输入" |

**功能表要点**:

| $\overline{EI}$ | 输入情况 | $\overline{A_2}\overline{A_1}\overline{A_0}$ | $\overline{EO}$ | $\overline{GS}$ |
| ---------------- | -------- | -------------------------------------------- | --------------- | --------------- |
| 1 | 任意 | 111(禁止) | 1 | 1 |
| 0 | 全为 1(无有效输入) | 111 | 0 | 1 |
| 0 | $\overline{I_7} = 0$ | 000(即 $I_7$ 的码 111 的反码) | 1 | 0 |
| 0 | $\overline{I_6} = 0$, 其余为 1 | 001 | 1 | 0 |
| 0 | $\overline{I_0} = 0$, 其余为 1 | 111? 不对, 应为 111 的反码 111 → 见下 | 1 | 0 |

**注意反码输出**:$\overline{I_0}$ 有效时输出 $\overline{A_2}\overline{A_1}\overline{A_0} = 111$(对应原码 000),$\overline{I_7}$ 有效时输出 $000$(对应原码 111). 因此读数时要**整体取反**才是真实编码.

**$\overline{EO}$ 与 $\overline{GS}$ 的用途**:

- $\overline{GS} = 0$ 表示至少有一个输入有效, 可用于判断"是否有键按下".  
- $\overline{EO} = 0$ 表示本片无有效输入, 用于级联时把低位片的 $\overline{EO}$ 接到高位片的 $\overline{EI}$, 实现优先级从高到低的传递.  

## 四、级联扩展: 16 线-4 线优先编码器

**思路**: 用两片 74LS148.

1. 高位片的 $\overline{EI}$ 接地(常有效), 其 8 个输入接 $\overline{I_{15}}$ 至 $\overline{I_8}$.  
2. 高位片的 $\overline{EO}$ 接低位片的 $\overline{EI}$, 低位片输入接 $\overline{I_7}$ 至 $\overline{I_0}$.  
3. 只有当高位片无有效输入时, 低位片才被允许编码, 从而保证高位片优先级更高.  
4. 输出的低 3 位由两片的输出经与门合并(未工作的那片输出为 111, 与门使之不影响结果).  
5. 最高位(第 4 位) 由高位片的 $\overline{GS}$ 提供: 高位片有编码时该位为 1.  

## 五、编码器的典型应用

1. **键盘编码**: 把 $N$ 个按键编码为 $\log_2 N$ 位二进制码, 是最经典的用途.  
2. **中断请求优先级裁决**: 多个中断源的优先级排队本质上就是优先编码.  
3. **地址生成**: 把多个设备的请求转换为设备地址.  

## 六、编码器与译码器的关系

| 对比项 | 编码器 | 译码器 |
| ------ | ------ | ------ |
| 方向 | 多输入 → 少输出($2^n \to n$) | 少输入 → 多输出($n \to 2^n$) |
| 输入特征 | 多个输入中仅一个有效 | 输入是一个二进制码 |
| 输出特征 | 输出是二进制码 | 输出中仅一个有效 |
| 是否唯一 | 普通编码器存在多输入冲突 | 输出唯一 |
| 典型芯片 | 74LS148 | 74LS138 |

## 易错点

- 74LS148 的输入和输出都是**低电平有效**, 输出是**反码**, 直接读数会得到错误结果.  
- 普通编码器在多个输入同时有效时输出错误, 因此实际系统必须用优先编码器或在输入端加锁存仲裁.  

## 相关笔记

- 逆运算:[译码器](./译码器.md)  
- 显示应用:[显示译码器](./显示译码器.md)  
- 设计中的应用:[组合逻辑电路的设计方法](./组合逻辑电路的设计方法.md)  
