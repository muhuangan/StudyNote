---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 3. 栈和队列 / 3.2 队列定义与实现

# 循环队列的顺序结构实现

**初始化**

```C
Queue* initQueue(){
    Queue *q = (Queue*)malloc(sizeof(Queue));
    q -> data = (ElemType*)malloc(sizeof(ElemType) * MAXSIZE);
    q -> front = 0;
    q -> rear = 0;
    return q;
}
```

**入队**

```C
int equeue(Queue *Q, ElemType e){
    if((Q -> rear + 1) % MAXSIZE == Q -> front){
        printf("满了\n");
        return 0;
    }
    Q -> data[Q -> rear] = e;
    Q -> rear = (Q -> rear + 1) % MAXSIZE;
    return 1;
}
```

注意, 这么写时一定有一个空间无法被利用到, 这是为了区分满队列和空队列的必要牺牲

**出队**

```C
int dequeue(Queue *Q, ElemType *e){
    if(Q -> front == Q -> rear){
        printf("空的\n");
        return 0;
    }
    *e = Q -> data[Q -> front];
    Q -> front = (Q -> front + 1) % MAXSIZE;
    return 1;
}
```

## 相关笔记

- 上一篇:[队列的顺序结构实现](./队列的顺序结构实现.md)
- 下一篇:[队列的链式结构](./队列的链式结构.md)
- 返回索引:[数据结构](./数据结构.md)
