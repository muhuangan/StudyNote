---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 4. 字符串与特殊矩阵压缩 / 4.1 字符串匹配

# 朴素匹配

**模式串**: 被查找的字符串

逐个尝试匹配字符串和模式串, 如果不匹配则把模式串往后挪一个位置, 直到完全匹配

```C
#include <stdio.h>
#include <string.h>
int strMatch(char* str, char* pattern){
    int n = strlen(str);
    int m = strlen(pattern);

    for(int i = 0; i <= (n - m); i++){
        int j = 0;
        whiel(j < m){
            if(str[i] == pattern[j]){
                i++;
                j++;
            }
            else{
                i = i - j;
                break;
            }
        }
        if(j == m){
            return i - j;
        }
    }
    return -1;
}

int main(){
    char* str = "abcabaabcabc";
    char* pattern = "abaa";
    int pos = strMatch(str, pattern);
    printf("%d\n", pos);

    return 0;
}
```

## 相关笔记

- 上一篇:[中缀表达式如何转换为后缀表达式](./中缀表达式如何转换为后缀表达式.md)
- 下一篇:[KMP算法](./KMP算法.md)
- 返回索引:[数据结构](./数据结构.md)
