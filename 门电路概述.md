---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第三章 门电路 / 3.1 概述

# 门电路概述

## 核心结论

门电路是实现基本逻辑运算的单元电路. 评价一个门电路不看它"能不能实现逻辑功能", 而看它的**电气特性**: 电平容限, 噪声容限, 扇出系数, 传输延迟和功耗.

## 一、门电路的分类

**按制造工艺**:

| 类别 | 代表系列 | 特点 |
| ---- | -------- | ---- |
| 双极型 | TTL(74 系列), ECL, I²L | 速度快, 驱动能力强, 功耗大 |
| 单极型(MOS) | CMOS(4000,74HC), NMOS, PMOS | 功耗极低, 集成度高, 速度早期较慢 |

**按功能**: 与门, 或门, 非门, 与非门, 或非门, 异或门, 同或门, 与或非门.

**按输出结构**:

1. **互补输出(推挽)**: 高低电平都有主动驱动, 是绝大多数门的标准结构.  
2. **集电极开路/漏极开路(OC/OD)**: 输出端只能主动拉低, 需要外接上拉电阻.  
3. **三态输出**: 除高, 低电平外还有**高阻态**, 用于总线.  

## 二、高电平与低电平的定义

实际的门电路并不输出精确的 0 V 和 5 V, 而是输出一个范围:

| 参数 | 含义 |
| ---- | ---- |
| $V_{OH}$ | 输出高电平(典型值, 如 3.4 V) |
| $V_{OL}$ | 输出低电平(典型值, 如 0.2 V) |
| $V_{IH}$ | 输入被识别为高电平的最低电压(如 2.0 V) |
| $V_{IL}$ | 输入被识别为低电平的最高电压(如 0.8 V) |

**关键关系**: 必须满足 $V_{OH} > V_{IH}$ 且 $V_{OL} < V_{IL}$, 否则前后级无法可靠配合. 这个差值就是噪声容限的来源.

## 三、噪声容限

$$V_{NH} = V_{OH(min)} - V_{IH(min)}$$

$$V_{NL} = V_{IL(max)} - V_{OL(max)}$$

其中 $V_{NH}$ 是高电平噪声容限,$V_{NL}$ 是低电平噪声容限. 噪声容限越大, 电路抗干扰能力越强. CMOS 电路因为 $V_{OH}$ 接近电源,$V_{OL}$ 接近地, 噪声容限明显优于 TTL.

## 四、扇出系数

**定义**: 一个门在正常工作时能够驱动的同类门的个数, 记为 $N$.

$$N = \min\left( \frac{I_{OH}}{I_{IH}},\ \frac{I_{OL}}{I_{IL}} \right)$$

其中 $I_{OH}$ 是输出高电平时能提供的最大拉电流,$I_{IH}$ 是每个负载门的输入高电平电流;$I_{OL}$ 是输出低电平时能吸收的最大灌电流,$I_{IL}$ 是每个负载门的输入低电平电流.

扇出本质上是**电流驱动能力**的问题, 而不是电压问题. CMOS 门的输入电流极小(可以认为为 0), 因此扇出系数可达 50 以上; TTL 门输入电流较大, 典型扇出为 10.

## 五、传输延迟

$$t_{pd} = \frac{t_{PHL} + t_{PLH}}{2}$$

其中 $t_{PHL}$ 是输出由高变低的延迟时间,$t_{PLH}$ 是输出由低变高的延迟时间. 延迟来源于器件内部的寄生电容充放电和载流子的存储效应.

## 六、功耗

1. **静态功耗**: 输出稳定时的功耗. CMOS 静态功耗几乎为 0(只有漏电流), 这是它最大的优势; TTL 静态功耗较大.  
2. **动态功耗**: 状态翻转瞬间的功耗, 主要由两部分组成: 对负载电容充放电的功耗 $C_L V_{DD}^2 f$, 以及两管瞬间同时导通产生的短路功耗.  
3. CMOS 的功耗几乎全部是动态功耗, 因此**功耗随工作频率线性增长**.  

## 七、主要系列性能对比

| 系列 | 电源电压 | 传输延迟 | 静态功耗(每门) | 噪声容限 | 扇出 |
| ---- | -------- | -------- | ---------------- | -------- | ---- |
| 74 标准 TTL | 5 V | 约 10 ns | 约 10 mW | 约 0.4 V | 10 |
| 74LS | 5 V | 约 9 ns | 约 2 mW | 约 0.4 V | 20 |
| 74HC(高速 CMOS) | 2 至 6 V | 约 8 ns | 约 0(微安级漏电) | 约 1.4 V(5 V 供电) | 50 以上 |
| 4000(标准 CMOS) | 3 至 15 V | 约 45 ns | 约 0 | 约 2 V(10 V 供电) | 50 以上 |
| 74HCT | 5 V | 约 9 ns | 约 0 | TTL 电平兼容 | 50 以上 |

## 八、选型经验

1. 追求低功耗, 高集成度 → CMOS.  
2. 需要驱动大电流负载, 环境干扰强 → TTL 或 CMOS 的 74HCT 兼容系列.  
3. 需要线与, 电平转换 → OC/OD 门加外接上拉电阻.  
4. 需要挂总线 → 三态门.  

## 易错点

- "CMOS 功耗为 0"只在静态成立, 高频工作下动态功耗可能超过 TTL.  
- 扇出系数取两个方向的**较小值**, 只看一个方向会得出错误的结论.  

## 相关笔记

- 最早的门电路形式:[半导体二极管门电路](./半导体二极管门电路.md)  
- 主流 CMOS 结构:[CMOS反相器](./CMOS反相器.md)  
- 主流双极型结构:[TTL反相器](./TTL反相器.md)  
