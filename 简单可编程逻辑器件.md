---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第八章 可编程逻辑器件 / 8.2 至 8.4 FPLA, PAL, GAL

# 简单可编程逻辑器件

## 核心结论

简单 PLD(SPLD) 的演化路线是 **FPLA → PAL → GAL**: FPLA 与或阵列都可编程(最灵活但慢), PAL 只让与阵列可编程(简化结构, 提高速度), GAL 在 PAL 基础上把输出电路做成**可编程的输出逻辑宏单元 OLMC** 并改用 EEPROM 工艺(可重复编程).

## 一、FPLA(现场可编程逻辑阵列)

**结构**:**与阵列和或阵列都可编程**.

**优点**:

1. 灵活性最高: 乘积项可以自由分配给任意输出, 资源利用率最高.  
2. 可以实现乘积项共享, 适合多输出函数.  

**缺点**:

1. 两级可编程阵列使**传输延迟较大**.  
2. 编程复杂, 需要同时决定两个阵列的连接.  
3. 缺少触发器, 主要用于组合逻辑.  

**现状**: 已被 PAL, GAL 取代, 但其"两级可编程"的思想保留在 CPLD 中.

## 二、PAL(可编程阵列逻辑)

**结构**:**与阵列可编程, 或阵列固定**.

**改进点**: 把或阵列固定后, 每个输出对应的乘积项数量在出厂时就已确定(如每个输出 4 至 8 个乘积项), 因此:

1. 延迟显著减小(只有一级可编程).  
2. 编程简单(只需决定与阵列).  
3. 成本更低.  

**代价**: 乘积项不能在各输出间共享, 资源利用率低于 FPLA; 若某函数需要的乘积项多于该输出端口的容量, 就无法实现.

**PAL 的输出结构(三种基本类型)**:

| 类型 | 特点 |
| ---- | ---- |
| 专用组合输出 | 无三态控制, 只能作输出 |
| 可编程 I/O 输出 | 带三态控制, 可作输入, 输出或双向 |
| 带反馈的寄存器输出 | 内含 D 触发器, 输出经触发器后可反馈回与阵列, 用于实现时序逻辑 |

**工艺**: 多为**熔丝型**, 只能编程一次(OTP, One Time Programmable). 这给开发调试带来不便—写错了芯片就报废.

## 三、GAL(通用阵列逻辑)

**结构**: 与 PAL 相同(**与阵列可编程, 或阵列固定**), 但有两项关键改进.

### 改进一: 输出逻辑宏单元(OLMC)

OLMC 是 GAL 的核心, 每个输出端配一个, 可由用户配置为多种工作模式:

| 配置项 | 可选 |
| ------ | ---- |
| 输出极性 | 高电平有效 / 低电平有效(可编程异或门) |
| 输出类型 | 组合输出 / 寄存器输出(D 触发器) |
| 输出使能 | 固定使能 / 由乘积项控制(三态) |
| 反馈通路 | 本端反馈 / 邻端反馈 / 无反馈 |

**意义**: 同一种 GAL 芯片可以模拟多种不同型号的 PAL, 这就是"通用"的含义.

### 改进二: EEPROM 工艺

- 可**电擦除, 重复编程**(通常 100 次以上), 彻底解决了 PAL 的一次性编程问题.  
- 支持**加密**(加密位写 1 后无法读出内部阵列, 保护知识产权).  

**典型器件**: GAL16V8(16 个输入端, 8 个输出端), GAL20V8, GAL22V10, ispGAL22V10.

## 四、三种 SPLD 的对比

| 对比项 | FPLA | PAL | GAL |
| ------ | ---- | --- | --- |
| 与阵列 | 可编程 | 可编程 | 可编程 |
| 或阵列 | 可编程 | 固定 | 固定(受 OLMC 控制) |
| 输出电路 | 固定 | 固定(按型号) | **可编程(OLMC)** |
| 工艺 | 熔丝 / EPROM | 熔丝 | **EEPROM** |
| 可重复编程 | 视工艺 | 否 | **是** |
| 速度 | 慢 | 快 | 快 |
| 灵活性 | 最高 | 低 | 高 |

## 五、用 SPLD 实现逻辑函数的步骤

1. 把函数化简为**最简与或式**(SPLD 的资源有限, 必须先化简).  
2. 统计各输出所需的乘积项个数, 检查是否超过器件每个输出端的容量.  
3. 若超出, 需要分解函数(用两级 SPLD 或改选更大器件).  
4. 在与阵列图上标出各乘积项所需的连接(叉号).  
5. 配置输出电路(GAL 需配置 OLMC 的模式).  
6. 生成 JEDEC 文件(`.jed`) 并烧录.  

## 六、典型应用场景

1. **地址译码**: 把 CPU 的高位地址译成各外设的片选信号, 是 SPLD 最经典的用途.  
2. **胶合逻辑**: 替代若干片 74 系列芯片, 减少面积与功耗.  
3. **状态机**: 用带寄存器输出的 GAL 实现简单的状态机.  
4. **接口转换**: 总线宽度, 时序的转换.  

## 七、SPLD 的局限

1. **资源有限**: 乘积项数量与触发器数量都很有限, 无法实现复杂逻辑.  
2. **时序能力弱**: 触发器数量少, 难以实现流水线与复杂数据通路.  
3. **无内嵌存储与算术单元**: 不能实现需要大量 RAM 或乘法器的功能.  

这些局限直接催生了 CPLD 与 FPGA.

## 易错点

- 认为 PAL 与 GAL 结构完全不同: 两者的阵列结构相同, GAL 的改进在**输出电路(OLMC) 与工艺(EEPROM)**上.  
- 用 SPLD 实现函数时忘记先化简: SPLD 的乘积项资源非常有限, 不化简很可能装不下.  

## 相关笔记

- 前置:[可编程逻辑器件概述](./可编程逻辑器件概述.md)  
- 后继器件:[复杂可编程逻辑器件](./复杂可编程逻辑器件.md),[现场可编程门阵列](./现场可编程门阵列.md)  
- 编程方式:[PLD的编程](./PLD的编程.md)  
