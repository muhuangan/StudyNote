---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 8. 排序 / 8.1 简单的排序算法

# 冒泡排序

**排序**: 按一组数据元素按照某种规则(通常是关键字的大小) 重新排列成有序的过程

**排序的三大指标**:

1. 时间复杂度
2. 空间复杂度
3. 排序稳定性(相等元素排序前后相对顺序是否保持不变)



遍历每个元素, 比较相邻元素的大小, 如果前面的元素比后面的元素大就交换位置, 重复几轮

```C
#include <stdio.h>
void swap(int* data, int m, int n){
    int temp = data[m];
    data[m] = data[n];
    data[n] = temp;
}

void bubbleSort(int* data, int len){
    //外层循环控制"趟数",共需len次比较
    for(int i = 0; i < len; i++){
        //内层循环控制每躺中相邻元素的比较和交换
        //每次比较到len - 1 - i,因为每一趟都能多出一个排序好的元素
        for(int j = 0; j < len - 1 - i; j++){
            if(data[j] > data[j + 1]){
                swap(data, j, j + 1);
            }
        }
    }
}

int main(){
    int data[] = {47, 35, 60, 95, 77, 15, 28};

    int len = sizeof(data) / sizeof(data[0]);

    bubbleSort(data, len);

    for(int i = 0; i < len; i++){
        printf("%d ", data[i]);
    }

    return 0;
}
```

| 维度       | 描述                                       |
| ---------- | ------------------------------------------ |
| 时间复杂度 | 最坏: $O(n^2)$ 最好: $O(n)$ 平均: $O(n^2)$ |
| 空间复杂度 | $O(1)$                                     |
| 算法稳定性 | 稳定                                       |
| 适用场景   | 数据量较小, 对稳定性有要求的场景           |

## 相关笔记

- 上一篇:[影响散列表查找性能的因素](./影响散列表查找性能的因素.md)  
- 下一篇:[插入排序](./插入排序.md)  
- 返回索引:[数据结构](./数据结构.md)  
