---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 5. 树与二叉树

# 森林与二叉树的转换

**森林转换成二叉树**

1. 将森林中的每一棵树都先转换为二叉树
2. 第一棵二叉树不动, 从第二棵树开始, 依次把后一棵二叉树的根结点作为前一棵二叉树的根结点的右孩子, 然后用线连起来

**二叉树转换为森林**

1. 从根结点开始, 若右孩子存在, 则把右孩子结点的连线删除, 然后把互不相连的拆开
2. 接下来把每一个独立的二叉树转换为树

**树的遍历**

1. 先根遍历(类似前序遍历)
2. 后根遍历(类似后序遍历)

**一棵树的后根遍历结果与这棵树所转化成的二叉树的中序遍历结果相同**

**一棵树的前根遍历结果与这棵树所转化成的二叉树的前序遍历结果相同**

树, 森林, 二叉树遍历结果关系如下图所示

| 树       | 森林     | 二叉树   |
| -------- | -------- | -------- |
| 先根遍历 | 前序遍历 | 前序遍历 |
| 后根遍历 | 中序遍历 | 中序遍历 |

**一棵树的结点数都比边数大一**

**计算二叉树的最大深度(也可用于层序遍历, 则一层一层的遍历)**  
典型实现

```C
int maxDepth(TreeNode* root){
    if(root == NULL){
        return 0;
    }

    int depth = 0;
    Queue *q = initQueue();
    equeue(q, root);

    while(!isEmpty(q)){
        int count = queueSize(q); //记录队列中的元素个数
        while(count > 0){
            TreeNode* curr;
            dequeue(q, &curr);
            if(curr -> lchild != NULL){
                equeue(q, curr -> lchild);
            }
            if(curr -> rchild != NULL){
                equeue(q, curr -> rchild);
            }
            count--;
        }
        depth++;
    }
    return depth;
}

//获取队列的元素数量
int queueSize(Queue *Q){
    if(!isEmpty(Q)){
        return Q -> rear - Q -> front;
    }
    else{
        return 0;
    }
}
```

**计算二叉树的带权路径长度**

1. 利用层序遍历获取路径长度
2. 判断是否为叶子结点
3. 是就计算带权路径长度
4. 全部加起来

## 相关笔记

- 上一篇:[树与二叉树的转换](./树与二叉树的转换.md)
- 下一篇:[图的基本概念](./图的基本概念.md)
- 返回索引:[数据结构](./数据结构.md)
