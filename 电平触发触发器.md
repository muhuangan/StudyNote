---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第五章 触发器 / 5.3 电平触发的触发器

# 电平触发触发器

## 核心结论

电平触发触发器在时钟为有效电平的**整个期间**都处于"透明"状态, 输出随输入变化. 它解决了"何时允许改变状态"的问题, 但引入了**空翻**—在一个时钟周期内可能翻转多次, 因此不能用于计数器等需要严格计数的场合.

## 一、电平触发 SR 触发器

**结构**: 在基本 SR 锁存器的输入端加一级由时钟 $CLK$ 控制的门.

$$S' = S \cdot CLK,\quad R' = R \cdot CLK$$

| $CLK$ | 行为                                   |
| ----- | -------------------------------------- |
| 0     | 输入被封锁,$S' = R' = 0$, 状态**保持** |
| 1     | 输入被接收, 按 SR 规则动作(**透明**)   |

**特性表**($CLK = 1$ 时):

| S   | R   | Q        | $Q^*$      |
| --- | --- | -------- | ---------- |
| 0   | 0   | 0        | 0(保持)    |
| 0   | 0   | 1        | 1(保持)    |
| 0   | 1   | $\times$ | 0(置 0)    |
| 1   | 0   | $\times$ | 1(置 1)    |
| 1   | 1   | $\times$ | 不定(禁止) |

**特性方程**($CLK = 1$ 时):

$$Q^* = S + \overline{R}Q,\quad SR = 0$$

## 二、电平触发 D 触发器(D 锁存器)

**结构**: 在电平触发 SR 触发器的输入端加一个反相器, 使 $S = D$,$R = \overline{D}$.

**特性方程**:

$$Q^* = D \quad (\text{当 } CLK = 1)$$

| $CLK$ | D        | $Q^*$     |
| ----- | -------- | --------- |
| 0     | $\times$ | $Q$(保持) |
| 1     | 0        | 0         |
| 1     | 1        | 1         |

**彻底消除了禁止状态**, 因为 $S$ 与 $R$ 恒互补,$SR = 0$ 自动满足. 这是 D 触发器成为最常用触发器的原因之一.

## 三、空翻现象(核心问题)

**定义**: 在一个时钟有效电平期间, 若输入信号发生变化, 触发器输出会**随之多次翻转**, 这种现象称为空翻.

**示例**: 把电平触发 D 触发器的 $\overline{Q}$ 反馈到 $D$(构成 T' 触发器, 用于计数), 当 $CLK = 1$ 时:

1. 初始 $Q = 0$, 故 $D = \overline{Q} = 1$, 输出翻转为 1.
2. 输出变为 1 后,$D = \overline{Q}$ 变为 0, 输出又翻转为 0.
3. 如此循环, 只要 $CLK = 1$ 持续, 输出就会以门延迟为周期**不停振荡**, 直到 $CLK$ 回到 0 时停在某一个不确定的状态.

**后果**: 触发器无法实现"每来一个时钟脉冲翻转一次"的计数功能, 输出状态不可预测.

## 四、空翻的条件

空翻只在同时满足以下两个条件时发生:

1. 触发器是**电平敏感**的(透明).
2. 触发器的**输出反馈到了输入**(如 T' 接法, 计数器, 移位寄存器链).

对于纯数据锁存($D$ 来自外部独立信号, 不随 $Q$ 变化), 电平触发不会空翻, 这也是 D 锁存器仍然可用的原因.

## 五、解决空翻的三条路线

| 路线               | 做法                         | 结果                   |
| ------------------ | ---------------------------- | ---------------------- |
| 缩短有效电平时间   | 把 $CLK$ 的有效电平变得极短  | 受器件参数限制, 不可靠 |
| 脉冲触发(主从结构) | 用主, 从两级锁存器, 交替工作 | 每个时钟周期只翻转一次 |
| 边沿触发           | 只在时钟跳变瞬间采样         | 最彻底, 现代标准       |

## 六、电平触发与边沿触发的关键差异

| 对比项   | 电平触发(透明)           | 边沿触发                                  |
| -------- | ------------------------ | ----------------------------------------- |
| 有效时刻 | 整个有效电平期间         | 仅在跳变沿瞬间                            |
| 输入毛刺 | 会被记录并传播           | 只有恰好落在建立/保持窗口内的才可能被采样 |
| 反馈应用 | 会空翻                   | 可以安全用于计数, 移位                    |
| 时序分析 | 复杂(需考虑整个电平宽度) | 简单(只需满足建立/保持时间)               |
| 典型用途 | 数据锁存, 地址锁存       | 寄存器, 计数器, 状态机                    |

## 七、仍然使用电平触发的场合

1. **地址锁存器**: 如 74LS373, 用 $ALE$ 信号锁存地址, 属于典型的数据锁存应用.
2. **透明锁存**: 需要在使能期间持续跟随输入的场合(如总线保持).
3. **异步置位/复位**: 触发器内部的异步控制端本质上是电平敏感的.

## 八、集成器件的电平标识

| 符号                  | 含义                               |
| --------------------- | ---------------------------------- |
| $CLK$ 端无标记或带"1" | 高电平有效(上升沿到下降沿期间透明) |
| $CLK$ 端带小圆圈      | 低电平有效                         |
| $CLK$ 端带三角符号    | 边沿触发(与电平触发区分的关键标志) |
| 三角符号加小圆圈      | 下降沿触发                         |

## 易错点

- 把电平触发 D 触发器当作边沿触发器使用, 在计数器设计中会导致空翻, 这是初学者的典型错误.
- 认为"只要时钟频率低就不会空翻": 空翻取决于有效电平**持续时间**与门延迟的比值, 降低频率并不能根本解决.

## 相关笔记

- 前置:[SR锁存器](./SR锁存器.md)
- 解决空翻的方案一:[脉冲触发触发器](./脉冲触发触发器.md)
- 解决空翻的方案二:[边沿触发触发器](./边沿触发触发器.md)
