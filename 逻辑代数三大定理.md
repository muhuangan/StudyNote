---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第二章 逻辑代数基础 / 2.5 逻辑代数的基本定理

# 逻辑代数三大定理

## 核心结论

**代入定理**保证公式可以推广,**反演定理**用于求反函数,**对偶定理**保证公式成对出现("记一得二"). 三者共同构成逻辑代数推导的合法性基础.

## 一、代入定理

**内容**: 在任何一个逻辑等式中, 将等式两边所有出现某一变量 $A$ 的位置, 都代之以同一个逻辑函数 $F$, 等式仍然成立.

**意义**: 把基本公式中的变量推广为任意复杂的表达式, 从而把二变量公式变成多变量公式.

例: 由德摩根定律 $\overline{A + B} = \overline{A}\,\overline{B}$, 令 $B = CD$, 则

$$\overline{A + CD} = \overline{A} \cdot \overline{CD} = \overline{A}(\overline{C} + \overline{D})$$

这就是多变量德摩根定律的来源.

**使用注意**:

1. 代入必须是"全部"出现的位置, 不能只替换一部分.  
2. 代入的必须是同一个逻辑函数.  
3. 代入后运算优先级可能发生变化, 必要时补充括号.  

## 二、反演定理

**内容**: 对于任意一个逻辑函数 $F$, 做如下变换得到的新函数就是原函数的反函数 $\overline{F}$:

1. 所有的与($\cdot$) 换成或($+$), 或($+$) 换成与($\cdot$);  
2. 所有的原变量换成反变量, 反变量换成原变量;  
3. 所有的 0 换成 1,1 换成 0.  

**两条必须遵守的附加规则**:

1. **保持原有的运算优先顺序**, 必要时添加括号(不能随意改变运算次序).  
2. **长非号(跨越两个及以上变量的非号) 保持不变**, 只对非号下面的表达式做变换.  

例: 求 $F = A\overline{B} + CD$ 的反函数.

$$\overline{F} = (\overline{A} + B)(\overline{C} + \overline{D})$$

例: 求 $F = \overline{A + \overline{B}C} \cdot D$ 的反函数. 长非号下的 $A + \overline{B}C$ 整体保留原结构, 仅对整体取反:

$$\overline{F} = \overline{\overline{A} \cdot (B + \overline{C})} + \overline{D} = A + \overline{B}C + \overline{D}$$

**与德摩根定律的关系**: 反演定理是德摩根定律的系统化推广, 德摩根定律处理单个运算, 反演定理处理整个函数.

## 三、对偶定理

**内容**: 对于任意一个逻辑函数 $F$, 做如下变换得到的新函数称为 $F$ 的**对偶式**, 记作 $F'$:

1. 与($\cdot$) 换成或($+$), 或($+$) 换成与($\cdot$);  
2. 0 换成 1,1 换成 0;  
3. **变量本身(原变量与反变量) 保持不变**.  

**对偶定理**: 若两个逻辑函数相等($F_1 = F_2$), 则它们的对偶式也相等($F_1' = F_2'$).

**意义**: 所有逻辑代数公式都是成对出现的, 记住"与形式"就能推出"或形式".

例: 由 $A + BC = (A + B)(A + C)$ 对偶可得 $A(B + C) = AB + AC$.

例: 由 $A + \overline{A}B = A + B$ 对偶可得 $A(\overline{A} + B) = AB$.

## 四、反演定理与对偶定理的区别

| 对比项 | 反演定理 | 对偶定理 |
| ------ | -------- | -------- |
| 运算符 | 与或互换 | 与或互换 |
| 常量 | 0 与 1 互换 | 0 与 1 互换 |
| 变量 | **原变量与反变量互换** | **变量保持不变** |
| 结果 | 得到 $\overline{F}$(反函数) | 得到 $F'$(对偶式, 与原函数一般不等) |
| 用途 | 求反函数, 求或非式 | 推出成对公式 |

这是最容易混淆的一对概念, 记住唯一区别就是**变量是否取反**.

## 五、由对偶定理得到的公式对

| 与形式 | 或形式(对偶) |
| ------ | -------------- |
| $A \cdot 0 = 0$ | $A + 1 = 1$ |
| $A \cdot 1 = A$ | $A + 0 = A$ |
| $AA = A$ | $A + A = A$ |
| $A\overline{A} = 0$ | $A + \overline{A} = 1$ |
| $A(B + C) = AB + AC$ | $A + BC = (A+B)(A+C)$ |
| $A(A + B) = A$ | $A + AB = A$ |
| $A(\overline{A} + B) = AB$ | $A + \overline{A}B = A + B$ |

## 易错点

- 使用反演定理时容易忘记"长非号不变", 把 $\overline{A + B}$ 内部的非号也一起翻掉.  
- 使用反演定理时运算优先级不能变, 若原式是 $AB + C$, 反演后应写成 $(\overline{A} + \overline{B})\overline{C}$, 不能写成 $\overline{A} + \overline{B}\,\overline{C}$.  

## 相关笔记

- 前置:[逻辑代数基本公式](./逻辑代数基本公式.md)  
- 反演定理的典型应用:[逻辑函数形式的变换](./逻辑函数形式的变换.md)  
