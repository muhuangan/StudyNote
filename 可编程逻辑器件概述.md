---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第八章 可编程逻辑器件 / 8.1 概述

# 可编程逻辑器件概述

## 核心结论

所有 PLD 的基本结构都是**"与阵列 + 或阵列 + 输出电路"**, 区别仅在于哪一部分可编程. 因为任意逻辑函数都能写成与或式, 所以这一结构可以实现任意组合逻辑; 再加上触发器即可实现时序逻辑.

## 一、PLD 的基本结构

```
输入 ──> 输入缓冲 ──> 与阵列 ──> 或阵列 ──> 输出电路 ──> 输出
                        ↑                      │
                        └────── 反馈 ──────────┘
```

| 组成部分 | 作用                                     |
| -------- | ---------------------------------------- |
| 输入缓冲 | 产生原变量与反变量, 并提供驱动能力       |
| 与阵列   | 产生乘积项(与项)                         |
| 或阵列   | 把乘积项相或, 得到与或式                 |
| 输出电路 | 三态输出, 寄存器输出, 极性选择, 反馈通路 |
| 反馈通路 | 把输出送回与阵列, 用于实现时序逻辑       |

**为什么"与阵列 + 或阵列"是通用的**: 任何逻辑函数都能表示为最小项之和(与或式), 因此这一结构在原理上是完备的.

## 二、分类: 按阵列的可编程性

| 器件        | 与阵列           | 或阵列     | 输出电路          |
| ----------- | ---------------- | ---------- | ----------------- |
| PROM        | 固定(全译码)     | **可编程** | 固定              |
| FPLA(PLA)   | **可编程**       | **可编程** | 固定              |
| PAL         | **可编程**       | 固定       | 固定(多种型号)    |
| GAL         | **可编程**       | 固定       | **可编程(OLMC)**  |
| EPLD / CPLD | 可编程(多逻辑块) | 固定       | 可编程宏单元      |
| FPGA        | 查找表(LUT)      | —          | 可配置逻辑块(CLB) |

**演化规律**: 从"或阵列可编程"走向"与阵列可编程", 再走向"输出电路可编程", 最后走向"整个逻辑块可配置". 每前进一步, 灵活性提高, 但结构与编程复杂度也提高.

## 三、分类: 按集成度

| 类别              | 器件             | 等效门数         | 特点                 |
| ----------------- | ---------------- | ---------------- | -------------------- |
| 低密度 PLD(SPLD)  | PLA, PAL, GAL    | 数百门           | 结构简单, 延迟可预测 |
| 高密度 PLD(HDPLD) | EPLD, CPLD, FPGA | 数千门至数千万门 | 可构建完整系统       |

## 四、阵列图的表示约定

PLD 的阵列图用三种交叉点符号表示连接关系:

| 符号         | 含义                               |
| ------------ | ---------------------------------- |
| 实心点 `•`   | **固定连接**(出厂时已连好, 不可改) |
| 叉号 `×`     | **可编程连接**(编程后连通)         |
| 交叉处无符号 | **不连接**                         |

**读图方法**: 先看哪些输入的"与线"上有连接, 确定各乘积项包含哪些变量, 再看或阵列把哪些乘积项相或.

## 五、编程工艺

| 工艺             | 可重复编程   | 是否易失               | 典型器件              |
| ---------------- | ------------ | ---------------------- | --------------------- |
| 熔丝(Fuse)       | 否(OTP)      | 否                     | 早期 PAL              |
| 反熔丝(Antifuse) | 否(OTP)      | 否                     | 部分 FPGA(抗辐射应用) |
| EPROM            | 是(紫外擦除) | 否                     | 早期 CPLD             |
| EEPROM / Flash   | 是(电擦除)   | 否                     | GAL, CPLD             |
| SRAM             | 是           | **是**(需外部配置芯片) | 主流 FPGA             |

**SRAM 工艺的代价**: 掉电后配置数据丢失, 必须在上电时从外部 Flash(或处理器) 加载配置. 好处是可无限次重配置, 甚至可以在运行时动态重构.

## 六、PLD 开发的一般流程

1. **设计输入**: 原理图输入或硬件描述语言(Verilog / VHDL).
2. **功能仿真**: 验证逻辑功能是否正确.
3. **综合**: 把设计映射为器件的逻辑资源(与或阵列, 宏单元, LUT).
4. **布局布线**: 确定资源的具体位置与连线.
5. **时序仿真**: 考虑实际延迟后的仿真.
6. **生成编程文件并下载**: 如 `.jed`(CPLD),`.bit`(FPGA).

## 七、PLD 与固定逻辑器件的对比

| 对比项   | 固定逻辑(74 系列) | PLD                                |
| -------- | ----------------- | ---------------------------------- |
| 灵活性   | 低(功能固定)      | 高(可编程)                         |
| 开发周期 | 长(需搭电路)      | 短(改代码即可)                     |
| 集成度   | 低                | 高                                 |
| 单位成本 | 大批量时低        | 小批量时低                         |
| 速度     | 快                | 视器件而定, 现代 FPGA 可达数百 MHz |
| 保密性   | 差(电路可见)      | 好(可加密)                         |

## 八、选型参考

| 需求                               | 推荐        |
| ---------------------------------- | ----------- |
| 小规模胶合逻辑, 上电即工作         | CPLD        |
| 大规模逻辑, 复杂时序, 需要 DSP/RAM | FPGA        |
| 航空, 抗辐射, 要求一次性烧死       | 反熔丝 FPGA |
| 大批量, 成本敏感                   | 转 ASIC     |

## 易错点

- 混淆 CPLD 与 FPGA:CPLD 基于**乘积项 + 宏单元**, 非易失, 上电即跑, 适合组合逻辑; FPGA 基于**查找表 + 触发器**, SRAM 易失, 需配置, 适合大规模时序逻辑.
- 认为 PLD 只能实现组合逻辑: 配合宏单元中的触发器, PLD 完全可以实现时序逻辑(状态机, 计数器等).

## 相关笔记

- ROM 与 PLD 的关系:[用存储器实现组合逻辑函数](./用存储器实现组合逻辑函数.md)
- 早期器件:[简单可编程逻辑器件](./简单可编程逻辑器件.md)
- 高密度器件:[复杂可编程逻辑器件](./复杂可编程逻辑器件.md),[现场可编程门阵列](./现场可编程门阵列.md)
- 编程方式:[PLD的编程](./PLD的编程.md)
