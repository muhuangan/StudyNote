---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第六章 时序逻辑电路 / 6.1 概述

# 时序逻辑电路概述

## 核心结论

时序逻辑电路 = **组合逻辑电路 + 存储电路(触发器)+ 反馈通路**. 输出由当前输入与电路现态共同决定, 因此具有记忆能力.

## 一、结构框图

```
输入 X ──┐
         ├──> 组合逻辑 ──> 输出 Y
         │       ↑
         │       │
         └──> 存储电路 Q ──┘(状态反馈)
```

信号分为三组:

1. **输入信号** $X$: 外部输入.  
2. **输出信号** $Y$: 对外输出.  
3. **驱动信号** $Z$ 与**状态信号** $Q$: 组合电路与存储电路之间的内部信号,$Z$ 是存储电路的输入,$Q$ 是存储电路的输出(即现态).  

**判据**: 时序电路中一定有存储电路, 其输出必须**反馈**到组合电路的输入端, 与输入信号共同决定输出.

## 二、三个方程

时序电路的行为由三个方程完整描述:

**输出方程**(组合电路部分):

$$Y = F(X,\ Q)$$

**驱动方程**(又称激励方程, 组合电路的输出):

$$Z = G(X,\ Q)$$

**状态方程**(把驱动方程代入触发器的特性方程得到):

$$Q^* = H(Z,\ Q)$$

这三个方程是分析和设计时序电路的通用语言.

## 三、同步与异步

| 对比项 | 同步时序电路 | 异步时序电路 |
| ------ | ------------ | ------------ |
| 时钟 | 所有触发器共用**同一个**时钟 | 触发器时钟不同, 或有的无时钟 |
| 状态更新 | **同时**更新 | **先后**更新, 存在级联延迟 |
| 速度 | 受最慢路径限制 | 某些情况下更快 |
| 设计难度 | 低(时序清晰) | 高(存在竞争, 状态不稳定) |
| 可靠性 | 高 | 较低 |
| 现状 | **绝对主流** | 仅用于简单分频等场合 |

**异步时序电路的特殊困难**: 由于触发器不是同时翻转, 电路可能出现不稳定的中间状态, 分析时必须考虑门延迟, 容易出现竞争-冒险.

## 四、Mealy 型与 Moore 型

**Mealy 型**: 输出取决于**输入和状态**.

$$Y = F(X,\ Q)$$

**Moore 型**: 输出**只取决于状态**.

$$Y = F(Q)$$

| 对比项 | Mealy 型 | Moore 型 |
| ------ | -------- | -------- |
| 输出方程 | $Y = F(X, Q)$ | $Y = F(Q)$ |
| 输出对输入的响应 | 输入变化**立即**反映到输出(不等时钟) | 必须等状态改变后才反映 |
| 输出毛刺 | 可能有(输入毛刺直接传到输出) | 无(输出只随状态变, 状态只在时钟沿变) |
| 所需状态数 | 通常较少 | 通常较多 |
| 时序分析 | 较难 | 简单 |

**工程倾向**: Moore 型输出经过触发器同步, 天然无毛刺, 时序干净, 因此在 FPGA 设计中更受推荐; Mealy 型状态数少, 响应快, 在状态机设计中也常用.

## 五、时序电路的分类总览

| 分类维度 | 类别 |
| -------- | ---- |
| 按时钟 | 同步, 异步 |
| 按输出依赖 | Mealy 型, Moore 型 |
| 按功能 | 寄存器, 计数器, 移位寄存器, 序列检测器, 状态机 |

## 六、与组合逻辑电路的关系

1. 组合电路是时序电路的**组成部分**(组合部分 + 存储部分).  
2. 时序电路的分析与设计都建立在组合电路的方法之上(写方程, 化简, 画逻辑图).  
3. 组合电路中的**竞争-冒险**在时序电路中表现为触发器输入端的毛刺, 但由于触发器只在时钟沿采样, 绝大部分毛刺被自然滤除—这正是同步设计的最大好处.  

## 七、典型时序电路一览

| 电路 | 核心结构 | 用途 |
| ---- | -------- | ---- |
| 寄存器 | 多个 D 触发器并联 | 暂存数据 |
| 移位寄存器 | D 触发器串联 | 串并转换, 延时, 序列产生 |
| 计数器 | 触发器 + 组合反馈 | 计数, 分频, 定时 |
| 顺序脉冲发生器 | 计数器 + 译码器 | 产生节拍脉冲 |
| 序列信号发生器 | 移位寄存器/计数器 + 组合逻辑 | 产生指定序列 |
| 状态机 | 状态寄存器 + 组合逻辑 | 控制逻辑的核心 |

## 易错点

- 认为"有触发器就是时序电路": 还必须有反馈通路, 且输出与状态有关; 纯粹的寄存器组(无反馈, 无组合逻辑) 只是存储阵列.  
- 认为 Moore 型电路不能有输入: Moore 型**有**输入(输入影响次态), 只是输出不直接依赖输入.  

## 相关笔记

- 存储元件基础:[触发器概述](./触发器概述.md)  
- 分析流程:[时序逻辑电路的分析方法](./时序逻辑电路的分析方法.md)  
- 对照:[组合逻辑电路概述](./组合逻辑电路概述.md)  
