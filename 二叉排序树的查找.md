---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 7. 查找 / 7.3 二叉排序树

# 二叉排序树的查找

二叉排序树(Binary Search Tree, BST) 是一种特殊的二叉树, 它通过特殊的节点排列方式实现了"查找效率高, 插入删除灵活"的目标

它的特性如下:

1. 对于树中任意一个节点, 其左子树上的所有节点的值都小于该节点的值
2. 对于树中任意一个节点, 其右子树所有节点的值都大于该节点的值, 并且左右子树本身也都是一颗二叉排序树

**折半查找判定树**:

1. 按照折半查找的思路可以生成一颗二叉排序树, 即折半查找判定树
2. 用中序遍历遍历一颗二叉树, 如果结果与数组匹配, 则说明该二叉树是数组的折半查找判定树
3. 折半查找时间复杂度(具有n个数据的查找表的时间复杂度为O($log_2 n$))
4. 二叉排序树的时间复杂度为O(n)(有可能是斜树)(为了解决这个问题, 平衡二叉树应运而生)
   **注意: 构造二叉排序树的目的并不是为了排序, 而是为了提高查找, 插入以及删除关键字的速度**


```C
#include <stdio.h>
#include <stdlib.h>
typedef int ElemType;
typedef struct TreeNode{
    ElemType data;
    struct TreeNode *lchild;
    struct TreeNode *rchild;
}TreeNode;

typedef TreeNode* BiTree;

int treeArr[] = {70, 55, 49, 30, -1, 39, -1, -1, 53, -1, -1, -1, 80, 75, -1, -1, 98, 95, -1, -1, -1};
int idx = 0;

void createTree(BiTree *T){
    ElemType num;
    num = treeArr[idx++];

    if(num == -1){
        *T = NULL;
    }
    else{
        *T = (BiTree)malloc(sizeof(TreeNode));
        (*T)->data = num;
        createTree(&(*T)->lchild);
        createTree(&(*T)->rchild);
    }
}

/**
 * @brief: 查找二叉排序树中是否含有指定值value
 * @param: T 当前子树根节点
 * @param: value 要查找的值
 * @param: parent 当前节点的父节点
 * @param: pos 指针变量用于返回找到的节点
 * @return 1 找到 0 未找到
**/
int search_bst(BiTree T, int value, BiTree parent, BiTree *pos){
    if(T == NULL){
        *pos = parent; //递归到空节点,查找失败,将父节点返回
        return 0;
    }

    if(T->data == value){
        *pos = T;
        return 1;
    }

    if(T->data > value){
        return search_bst(T->lchild, value, T, pos); // 要查找的值比当前节点小,往左子树找
    }
    else{
        return search_bst(T->rchild, value, T, pos); // 要查找的值比当前节点大,往右子树找
    }
}

void preOrder(BiTree T){
    if(T == NULL){
        return;
    }

    printf("%d ", T->data);
    preOrder(T->lchild);
    preOrder(T->rchild);
}

int main(){
    BiTree T;
    createTree(&T);

    BiTree searchT;
    search_bst(T, 53, NULL, &searchT);
    preOrder(T);
    printf("\n");

    printf("%d\n", searchT->data);
    return 0;
}
```

## 相关笔记

- 上一篇:[折半查找(线性表)](./折半查找.md)  
- 下一篇:[二叉排序树的插入](./二叉排序树的插入.md)  
- 返回索引:[数据结构](./数据结构.md)  
