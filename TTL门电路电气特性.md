---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第三章 门电路 / 3.5 TTL门电路 / 电气特性

# TTL门电路电气特性

## 核心结论

TTL 的四个关键参数是 **2.4 V / 0.4 V 的输出保证值**,**2.0 V / 0.8 V 的输入阈值**,**0.4 V 的噪声容限**和**10 的扇出系数**. 所有这些数值都应当使用手册中的**保证值**(最坏情况值), 而不是典型值.

## 一、四个电平参数

| 参数 | 符号 | 含义 | 74 系列保证值 |
| ---- | ---- | ---- | ------------- |
| 输出高电平 | $V_{OH}$ | 输出为 1 时的最低电压 | $\ge 2.4\ \text{V}$(典型 3.4 V) |
| 输出低电平 | $V_{OL}$ | 输出为 0 时的最高电压 | $\le 0.4\ \text{V}$(典型 0.2 V) |
| 输入高电平 | $V_{IH}$ | 被识别为 1 的最低输入电压 | $\ge 2.0\ \text{V}$ |
| 输入低电平 | $V_{IL}$ | 被识别为 0 的最高输入电压 | $\le 0.8\ \text{V}$ |

**注意 $V_{OH} > V_{IH}$ 且 $V_{OL} < V_{IL}$**: 这是前后级可靠级联的必要条件, 差值就是噪声容限.

## 二、噪声容限

$$V_{NH} = V_{OH(min)} - V_{IH(min)} = 2.4 - 2.0 = 0.4\ \text{V}$$

$$V_{NL} = V_{IL(max)} - V_{OL(max)} = 0.8 - 0.4 = 0.4\ \text{V}$$

TTL 的噪声容限只有 0.4 V, 明显小于 CMOS, 这是 TTL 在强干扰场合的短板.

## 三、输入电流参数

| 参数 | 符号 | 典型值 | 说明 |
| ---- | ---- | ------ | ---- |
| 输入低电平电流(短路电流) | $I_{IL}$ 或 $I_{IS}$ | $-1.6\ \text{mA}$ | 电流从输入端**流出** |
| 输入高电平电流(漏电流) | $I_{IH}$ | $40\ \mu\text{A}$ | 电流**流入**输入端 |
| 最大灌电流 | $I_{OL}$ | $16\ \text{mA}$ | 输出低电平时可吸收 |
| 最大拉电流 | $I_{OH}$ | $-400\ \mu\text{A}$ | 输出高电平时可提供(能力很弱) |

**关键洞察**: TTL 输出高电平时的拉电流能力(0.4 mA) 远小于输出低电平时的灌电流能力(16 mA), 相差 40 倍. 因此工程上应**尽量让 TTL 工作在灌电流状态**(如 LED 采用"输出低电平点亮"的接法).

## 四、扇出系数

$$N = \min\left( \frac{|I_{OL}|}{|I_{IL}|},\ \frac{|I_{OH}|}{|I_{IH}|} \right) = \min\left( \frac{16}{1.6},\ \frac{0.4}{0.04} \right) = \min(10,\ 10) = 10$$

即一个标准 TTL 门最多驱动 10 个同类门. 74LS 系列因输入电流更小, 扇出可达 20.

** fan-out 超限的后果**: 输出电平被拉出规定范围, 噪声容限消失, 电路间歇性误动作.

## 五、传输延迟

$$t_{pd} = \frac{t_{PHL} + t_{PLH}}{2}$$

标准 74 系列约 10 ns, 74LS 约 9 ns, 74S(肖特基) 约 3 ns.

**延迟的主要来源**:

1. 三极管的**存储时间**(饱和越深, 退出越慢), 这是标准 TTL 较慢的主因.  
2. 负载电容的充放电.  

**肖特基系列的改进**: 在 T2, T5 的基极与集电极之间并联**肖特基二极管**(抗饱和钳位), 使三极管无法进入深饱和, 从而消除存储时间, 把延迟降到 3 ns 量级. 这是 74S/74LS 系列的核心技术.

## 六、功耗

标准 74 系列每门约 10 mW, 74LS 约 2 mW, 74S 约 20 mW.

TTL 的功耗主要是**静态功耗**: 无论输出高电平还是低电平, 电源到地都存在直流通路(输出高电平时经 R1, T1, T2, R3 支路; 输出低电平时经 R1, T1, T2, T5 支路), 因此 TTL 的功耗**几乎不随频率变化**, 这与 CMOS 完全相反.

| 对比项 | TTL | CMOS |
| ------ | --- | ---- |
| 静态功耗 | 毫瓦级 | 微瓦级 |
| 功耗随频率的变化 | 基本不变 | 线性增长 |
| 低频应用 | 劣势 | 优势 |
| 高频应用 | 相对优势 | 劣势 |

## 七、交流噪声容限

除了直流噪声容限, 还需考虑**交流噪声容限**: 窄脉冲噪声因为门电路有传输延迟, 可能来不及响应而被自然滤除. 一般来说, 噪声脉冲宽度小于门的传输延迟时, 不会造成误动作. 这一特性在实际系统中提供了额外的抗干扰余量.

## 八、输入端处理的工程规则

| 情况 | 处理方式 |
| ---- | -------- |
| 与门, 与非门的多余输入端 | 接高电平(经 1 kΩ 上拉电阻接 $V_{CC}$), 或与使用端并联 |
| 或门, 或非门的多余输入端 | 接地, 或与使用端并联 |
| 悬空 | 等效高电平, 但**不规范**, 易受干扰, 应禁止 |
| 长线传输的输入端 | 加匹配电阻或施密特触发器整形 |

**与使用端并联的代价**: 会增加输入电容并使输入电流加倍, 因此在扇出紧张时应优先采用电阻上拉/下拉方式.

## 九、电源与去耦

- 电源必须在 4.75 V 至 5.25 V 范围内.  
- 每个芯片电源引脚处接 0.01 至 0.1 微法去耦电容.  
- TTL 输出级在翻转瞬间会产生很大的**尖峰电流**(推挽两管瞬间同时导通), 必须通过去耦电容就近吸收, 否则会在电源线上产生严重干扰.  

## 易错点

- 计算噪声容限必须用 $V_{OH(min)} = 2.4\ \text{V}$, 用典型值 3.4 V 会得出"噪声容限 1.4 V"的错误结论.  
- 扇出计算要取两个方向的较小值, 只算灌电流方向会高估驱动能力.  

## 相关笔记

- 电路原理:[TTL反相器](./TTL反相器.md)  
- 对比:[CMOS门电路电气特性](./CMOS门电路电气特性.md)  
- 参数定义的统一说明:[门电路概述](./门电路概述.md)  
