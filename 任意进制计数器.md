---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第六章 时序逻辑电路 / 6.3.2 用 MSI 计数器实现任意进制计数器

# 任意进制计数器

## 核心结论

用模 $N$ 的集成计数器实现模 $M$ 计数器, 有两条基本路线:**反馈清零法**(利用异步清零端) 与**反馈置数法**(利用同步置数端). 当 $M > N$ 时, 还要先用多片级联把模扩展为 $N_1 \times N_2 \ge M$.

## 一、两条路线的本质区别

| 对比项 | 反馈清零法 | 反馈置数法 |
| ------ | ---------- | ---------- |
| 利用的端口 | 异步清零端 $\overline{CR}$ | 同步置数端 $\overline{LD}$ |
| 动作时刻 | 状态一出现就**立即**清零 | 等**下一个时钟沿**才装入 |
| 是否有过渡状态 | **有**($M$ 这个状态瞬间出现又被清掉) | 无 |
| 计数区间 | 固定从 0 开始 | 可从任意预置值开始 |
| 可靠性 | 较低(依赖门的竞争, 毛刺敏感) | 高 |
| 典型芯片 | 74LS161,74LS160,74LS290 | 74LS161,74LS160 |

## 二、反馈清零法(异步清零)

**原理**: 令计数器计到状态 $M$ 时产生清零信号, 计数器立即回到 0000. 由于清零是异步的, 状态 $M$ 只存在极短的一瞬间(过渡状态), 实际计数循环是 $0, 1, \cdots, M-1$.

**步骤**:

1. 把状态 $M$ 写成二进制, 找出其中为 1 的各位.  
2. 把这些位的 $Q$ 端接到一个与门(输出低有效时接与非门), 输出接 $\overline{CR}$.  
3. 计数循环为 $0$ 至 $M-1$, 共 $M$ 个状态.  

**示例**: 用 74LS161(模 16) 构成模 10 计数器.

状态 $M = 10 = 1010$, 其中 $Q_3 = 1$,$Q_1 = 1$, 故

$$\overline{CR} = \overline{Q_3 Q_1}$$

即把 $Q_3$,$Q_1$ 接一个与非门, 输出接 $\overline{CR}$. 计数循环为 0000 至 1001(0 至 9).

**缺点分析**:

1. **存在过渡状态**: 状态 1010 会瞬间出现, 若对输出译码可能产生毛刺.  
2. **清零动作不可靠**: 由于各触发器的清零速度略有差异, 可能出现某一级未被清掉的情况, 导致状态错误. 改进方法是加一个**清零信号锁存**(用基本 SR 锁存器把清零脉冲展宽到半个时钟周期).  
3. 只能从 0 开始计数.  

## 三、反馈置数法(同步置数)

**原理**: 令计数器计到某一状态 $S_{end}$ 时产生置数信号, 在**下一个时钟沿**把预置值 $S_{start}$ 装入计数器. 计数循环为 $S_{start} \to \cdots \to S_{end} \to S_{start}$.

**循环长度**:

$$M = S_{end} - S_{start} + 1$$

**两种常用接法**:

**接法一: 自己产生状态检测逻辑**

1. 把 $S_{start}$ 接到数据端 $D_3$ 至 $D_0$.  
2. 用门电路检测 $S_{end}$, 输出接 $\overline{LD}$.  

**接法二: 利用进位输出 $RCO$(最常用)**

1. 把预置值设为 $N - M$($N$ 为芯片模).  
2. $RCO$ 反相后接 $\overline{LD}$(或直接接, 取决于 $RCO$ 有效电平).  
3. 计数器从 $N - M$ 开始, 计到 $N - 1$ 时 $RCO$ 有效, 下一个时钟沿装回 $N - M$, 循环长度恰为 $M$.  

**示例**: 用 74LS161($N = 16$) 构成模 10 计数器.

- 预置值 $D_3 D_2 D_1 D_0 = 16 - 10 = 6 = 0110$.  
- 把 $RCO$ 经反相(或直接, 视极性) 接到 $\overline{LD}$.  
- 计数循环为 $0110 \to 0111 \to \cdots \to 1111 \to 0110$, 共 10 个状态.  
- 若需要输出 0 至 9 的编码, 可在输出端再减 6(或改用接法一, 从 0000 计到 1001).  

**优点**: 无过渡状态, 可靠性高, 可从任意值开始计数.

## 四、$M > N$ 时的扩展

### 分解法($M$ 可分解为两个因数)

若 $M = M_1 \times M_2$, 则用两片分别构成模 $M_1$, 模 $M_2$ 计数器, 再级联.

**示例**: 模 60 计数器 = 模 6 × 模 10. 用一片 74LS161 构成模 6(作为高位, 计 0 至 5), 一片 74LS160 构成模 10(作为低位), 低位片的进位接高位片的使能(同步级联) 或时钟(异步级联).

### 整体反馈法($M$ 为质数或不能分解)

1. 先把两片级联成模 $N_1 \times N_2$(如 16 × 16 = 256).  
2. 再对整体用反馈清零或反馈置数法截取为模 $M$.  
3. 状态检测逻辑要覆盖两片的所有位.  

**示例**: 用两片 74LS161 构成模 100 计数器. 级联得模 256, 再用反馈置数法预置 $256 - 100 = 156$, 计到 255 时 $RCO$ 有效并置回 156, 循环长度为 100.

## 五、两种级联方式的比较

| 方式 | 接法 | 特点 |
| ---- | ---- | ---- |
| 同步级联 | 低位片 $RCO$ 接高位片使能端, 两片共用时钟 | 无过渡状态, 速度快,**推荐** |
| 异步级联 | 低位片 $RCO$(或 $Q$) 接高位片时钟 | 简单, 但有过渡状态, 高速时慎用 |

## 六、设计检查清单

1. 目标模 $M$ 是否小于等于芯片级联后的总模?  
2. 清零/置数信号的检测逻辑是否覆盖了所有为 1 的位?  
3. 若用异步清零, 是否考虑了过渡状态和毛刺?  
4. 预置值计算是否正确($N - M$ 而不是 $M$)?  
5. 级联时 $RCO$ 的极性与时序是否匹配?  
6. 使能端, 清零端是否都接了确定电平,**没有悬空**?  

## 七、常见问题排查

| 现象 | 原因 |
| ---- | ---- |
| 计数范围比预期多一个 | 异步清零的过渡状态被误计(应确认是异步还是同步动作) |
| 计数不到预期值就归零 | 检测逻辑漏接了某一位, 或与非门输入接错 |
| 高位片不计数 | $RCO$ 未接到高位片使能, 或使能端被接地 |
| 输出有毛刺 | 用了异步清零或异步级联 |

## 易错点

- 混淆异步清零与同步置数的动作时刻: 异步清零时状态 $M$ 不占用计数循环, 同步置数时状态 $S_{end}$ **占用**计数循环.  
- 预置值算反: 应装入 $N - M$ 而不是 $M$. 例如模 10 用 74LS161 时应预置 6(0110), 不是 10(1010).  

## 相关笔记

- 前置:[计数器](./计数器.md)  
- 器件细节:[寄存器与移位寄存器](./寄存器与移位寄存器.md)  
- 输出译码:[顺序脉冲发生器与序列信号发生器](./顺序脉冲发生器与序列信号发生器.md)  
