---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 8. 排序 / 8.1 简单的排序算法

# 简单选择排序

**排序**: 按一组数据元素按照某种规则(通常是关键字的大小) 重新排列成有序的过程

**排序的三大指标**:

1. 时间复杂度
2. 空间复杂度
3. 排序稳定性(相等元素排序前后相对顺序是否保持不变)


找最小数, 然后把最小数放在最前面

```C
#include <stdio.h>

void swap(int* data, int m, in n){
    int temp = data[m];
    data[m] = data[n];
    data[n] = tem;
}

void selectSort(int* data, int len){
    int min;
    // 外层循环:控制每一轮的起始位置
    for(int i = 0; i < len; i++){
        // 假设当前位置i是最小值
        min = i;
        // 内层循环:从当前位置i向后寻找最小值的位置
        for(int j = i; j < len; j++){
            // 如果发现比当前最小值还小的元素,更新最小值的位置
            if(data[min] > data[j]){
                min = j;
            }
        }
        // 如果找到的最小值不是当前位置,则进行交换
        if(i != min){
            swap(data, i, min);
        }
    }
}

int main(){
    int data[] = {47, 35, 60, 95, 77, 15, 28};
    int len = sizeof(data) / sizeof(data[0]);
    selectSort(data, len);

    for(int i = 0; i < len; i++){
        printf("%d", data[i]);
    }
    return 0;
}
```

| 维度       | 描述                                               |
| ---------- | -------------------------------------------------- |
| 时间复杂度 | $O(n^2)$                                           |
| 空间复杂度 | $O(1)$                                             |
| 算法稳定性 | 不稳定                                             |
| 适用场景   | 在小规模数据排序时, 选择排序简单直观, 便于快速实现 |

## 相关笔记

- 上一篇:[折半插入排序](./折半插入排序.md)  
- 下一篇:[堆的插入操作](./堆的插入操作.md)  
- 返回索引:[数据结构](./数据结构.md)  
