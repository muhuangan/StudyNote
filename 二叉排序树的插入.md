---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 7. 查找 / 7.3 二叉排序树

# 二叉排序树的插入

插入与查找通过观察不难发现是高度相似的

```C
#include <stdio.h>
#include <stdlib.h>
typedef int ElemType;
typedef struct TreeNode{
    ElemType data;
    struct TreeNode *lchild;
    struct TreeNode *rchild;
}TreeNode;

typedef TreeNode* BiTree;

/**
 * @brief: 查找二叉排序树中是否含有指定值value
 * @param: T 当前子树根节点
 * @param: value 要查找的值
 * @param: parent 当前节点的父节点
 * @param: pos 指针变量用于返回找到的节点
 * @return 1 找到 0 未找到
**/
int search_bst(BiTree T, int value, BiTree parent, BiTree *pos){
    if(T == NULL){
        *pos = parent; //递归到空节点,查找失败,将父节点返回
        return 0;
    }

    if(T->data == value){
        *pos = T;
        return 1;
    }

    if(T->data > value){
        return search_bst(T->lchild, value, T, pos); // 要查找的值比当前节点小,往左子树找
    }
    else{
        return search_bst(T->rchild, value, T, pos); // 要查找的值比当前节点大,往右子树找
    }
}

// 向二叉排序树中插入一个值
// 如果值已经存在,插入失败,返回0,否则插入成功,返回1,
int insert_bst(BiTree *T, int value){
    BiTree parent, pos;
    BiTree curr;

    //查找插入位置
    int status = search_bst(*T, value, NULL, &pos);
    if(status == 0){
        //如果没找到,创建新节点
        curr = (BiTree)malloc(sizeof(TreeNode));
        curr->data = value;
        curr->lchild = NULL;
        curr->rchild = NULL;
        //如果树为空,新节点作为根节点
        if(pos == NULL){
            *T = curr;
        }
        //插入为左子节点
        else if(value < pos->data){
            pos->lchild = curr;
        }
        else{
            pos->rchild = curr;
        }
        return 1;
    }
    else{
        // 如果已经存在
        return 0;
    }
}

void preOrder(BiTree T){
    if(T == NULL){
        return;
    }

    printf("%d ", T->data);
    preOrder(T->lchild);
    preOrder(T->rchild);
}

int main(){
    int i = 0;
    BiTree T = NULL;

    int treeArr[] = {70, 55, 49, 30, 53, 80, 75, 98, 95};

    for(int i = 0; i < 30; i++){
        insert_bst(&T, treeArr[i]);
    }

    preOrder(T);
    printf("\n");

    insert_bst(&T, 99);

    preOrder(T);
    printf("\n");

    return 0;
}
```

## 相关笔记

- 上一篇:[二叉排序树的查找](./二叉排序树的查找.md)
- 下一篇:[二叉排序树的删除](./二叉排序树的删除.md)
- 返回索引:[数据结构](./数据结构.md)
