---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第二章 逻辑代数基础 / 2.6 逻辑函数的表示方法 / 最小项与最大项

# 最小项与最大项

## 核心结论

**最小项**是包含全部 $n$ 个变量的乘积项,**最大项**是包含全部 $n$ 个变量的和项. 任何逻辑函数都能唯一地表示为"最小项之和"或"最大项之积", 两种标准形式互为补充.

## 一、最小项

**定义**:$n$ 个变量的最小项是包含这 $n$ 个变量(每个变量以原变量或反变量形式出现且仅出现一次) 的**乘积项**.

$n$ 个变量共有 $2^n$ 个最小项, 记作 $m_0, m_1, \cdots, m_{2^n-1}$.

**编号规则**: 把最小项中的原变量记为 1, 反变量记为 0, 得到的二进制数对应的十进制值就是该最小项的下标.

**三变量最小项对照表**:

| 编号 | 变量取值 (A B C) | 最小项 | 记号 |
| ---- | ---------------- | ------ | ---- |
| 0 | 000 | $\overline{A}\,\overline{B}\,\overline{C}$ | $m_0$ |
| 1 | 001 | $\overline{A}\,\overline{B}C$ | $m_1$ |
| 2 | 010 | $\overline{A}B\overline{C}$ | $m_2$ |
| 3 | 011 | $\overline{A}BC$ | $m_3$ |
| 4 | 100 | $A\overline{B}\,\overline{C}$ | $m_4$ |
| 5 | 101 | $A\overline{B}C$ | $m_5$ |
| 6 | 110 | $AB\overline{C}$ | $m_6$ |
| 7 | 111 | $ABC$ | $m_7$ |

**最小项的性质**:

1. 对于任意一组变量取值, 只有一个最小项为 1, 其余全为 0.  
2. 任意两个不同最小项的乘积为 0($m_i \cdot m_j = 0$,$i \ne j$).  
3. 全部最小项之和恒为 1($\sum_{i=0}^{2^n-1} m_i = 1$).  
4. 只有一个变量不同的两个最小项称为**相邻最小项**, 它们可以合并消去一个变量:$m_i + m_j$ 消去那个不同的变量.  

## 二、最大项

**定义**:$n$ 个变量的最大项是包含这 $n$ 个变量(每个变量以原变量或反变量形式出现且仅出现一次) 的**和项**.

$n$ 个变量共有 $2^n$ 个最大项, 记作 $M_0, M_1, \cdots, M_{2^n-1}$.

**编号规则**: 与最小项**相反**, 把最大项中的原变量记为 0, 反变量记为 1, 得到的二进制数对应的十进制值就是下标.

**三变量最大项对照表**:

| 编号 | 变量取值 (A B C) | 最大项 | 记号 |
| ---- | ---------------- | ------ | ---- |
| 0 | 000 | $A + B + C$ | $M_0$ |
| 1 | 001 | $A + B + \overline{C}$ | $M_1$ |
| 2 | 010 | $A + \overline{B} + C$ | $M_2$ |
| 3 | 011 | $A + \overline{B} + \overline{C}$ | $M_3$ |
| 4 | 100 | $\overline{A} + B + C$ | $M_4$ |
| 5 | 101 | $\overline{A} + B + \overline{C}$ | $M_5$ |
| 6 | 110 | $\overline{A} + \overline{B} + C$ | $M_6$ |
| 7 | 111 | $\overline{A} + \overline{B} + \overline{C}$ | $M_7$ |

**最大项的性质**:

1. 对于任意一组变量取值, 只有一个最大项为 0, 其余全为 1.  
2. 任意两个不同最大项之和为 1($M_i + M_j = 1$,$i \ne j$).  
3. 全部最大项之积恒为 0($\prod_{i=0}^{2^n-1} M_i = 0$).  

## 三、最小项与最大项的关系

$$m_i = \overline{M_i},\quad M_i = \overline{m_i}$$

即编号相同的最大项与最小项互为反函数.

## 四、两种标准形式

**标准与或式(最小项之和)**:

$$F = \sum m(\text{输出为 1 的那些最小项编号})$$

**标准或与式(最大项之积)**:

$$F = \prod M(\text{输出为 0 的那些最大项编号})$$

**互补关系**: 若 $F = \sum m(1, 3, 5)$, 则输出为 0 的编号是 0,2,4,6,7, 因此

$$F = \prod M(0, 2, 4, 6, 7)$$

两种写法描述的是同一个函数, 编号集合互补.

## 五、由一般式展开为标准与或式

**步骤**:

1. 对每一个乘积项, 找出缺失的变量 $X$.  
2. 用 $(X + \overline{X}) = 1$ 与该乘积项相乘, 展开成两个完整的最小项.  
3. 重复直到所有项都包含全部变量, 最后合并相同项.  

例: 把 $F = AB + \overline{A}C$(三变量 A, B, C) 展开为最小项之和.

- $AB$ 缺 $C$:$AB(C + \overline{C}) = ABC + AB\overline{C} = m_7 + m_6$  
- $\overline{A}C$ 缺 $B$:$\overline{A}C(B + \overline{B}) = \overline{A}BC + \overline{A}\,\overline{B}C = m_3 + m_1$  

所以 $F = \sum m(1, 3, 6, 7)$.

## 六、为什么要引入标准形式

1. 标准形式是**唯一的**, 便于判断两个函数是否等价.  
2. 它是卡诺图, 真值表之间转换的统一接口.  
3. 它是用译码器, 数据选择器实现任意逻辑函数的直接依据.  

## 易错点

- 最小项与最大项的编号规则相反, 这是最容易出错的地方: 最小项"原变量 = 1", 最大项"原变量 = 0".  
- 标准与或式列出的是**输出为 1** 的编号, 标准或与式列出的是**输出为 0** 的编号, 不要写反.  

## 相关笔记

- 前置:[逻辑函数的表示方法](./逻辑函数的表示方法.md)  
- 应用:[卡诺图化简法](./卡诺图化简法.md),[用中规模器件实现逻辑函数](./用中规模器件实现逻辑函数.md)  
