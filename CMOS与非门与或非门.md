---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第三章 门电路 / 3.3 CMOS门电路 / CMOS与非门、或非门

# CMOS与非门与或非门

## 核心结论

CMOS 与非门把**多个 NMOS 串联, 多个 PMOS 并联**; 或非门反之,**NMOS 并联, PMOS 串联**. 规律是:**NMOS 串实现与, NMOS 并实现或, PMOS 与之对偶, 最后整体天然反相**.

## 一、CMOS 与非门

**结构**(以二输入为例):

- 上拉部分: 两个 PMOS 管 **并联**在 $V_{DD}$ 与输出之间.  
- 下拉部分: 两个 NMOS 管 **串联**在输出与地之间.  
- 输入 $A$ 同时控制一个 PMOS 与一个 NMOS, 输入 $B$ 同理.  

**工作原理**:

| A | B | 下拉通路(NMOS 串联) | 上拉通路(PMOS 并联) | 输出 |
| - | - | --------------------- | --------------------- | ---- |
| 0 | 0 | 断开 | 两管全导通 | 1 |
| 0 | 1 | 断开 | A 管导通 | 1 |
| 1 | 0 | 断开 | B 管导通 | 1 |
| 1 | 1 | 两管全导通 | 断开 | 0 |

逻辑功能:$F = \overline{AB}$.

## 二、CMOS 或非门

**结构**:

- 上拉部分: 两个 PMOS 管 **串联**在 $V_{DD}$ 与输出之间.  
- 下拉部分: 两个 NMOS 管 **并联**在地与输出之间.  

| A | B | 下拉通路(NMOS 并联) | 上拉通路(PMOS 串联) | 输出 |
| - | - | --------------------- | --------------------- | ---- |
| 0 | 0 | 断开 | 两管全导通 | 1 |
| 0 | 1 | 导通 | 断开 | 0 |
| 1 | 0 | 导通 | 断开 | 0 |
| 1 | 1 | 导通 | 断开 | 0 |

逻辑功能:$F = \overline{A + B}$.

## 三、记忆规律

**下拉网络(NMOS) 决定"何时输出为 0"**:

- NMOS 串联 → 全部输入为 1 时才导通 → 输出为 0 的条件是"全 1" → 与非.  
- NMOS 并联 → 任一输入为 1 就导通 → 输出为 0 的条件是"有 1" → 或非.  

**上拉网络(PMOS) 与之对偶**: NMOS 串则 PMOS 并, NMOS 并则 PMOS 串.

**整体一定反相**: 因为下拉网络导通时输出为 0, 上拉网络导通时输出为 1, 两者互补, 所以基本 CMOS 门**天然是与非, 或非, 而不是与, 或**. 要实现与门, 或门, 必须在输出后再加一级反相器.

## 四、为什么 CMOS 偏爱与非门

1. 相同输入数下, 与非门的 PMOS 并联, 各管宽长比要求小, 总面积比或非门小.  
2. 电子迁移率大于空穴迁移率(约 2 至 3 倍), NMOS 的驱动能力强于同尺寸 PMOS; 串联 PMOS 会显著劣化上升沿, 因此尽量避免.  
3. 实际 CMOS 工艺中, 与非门是最"经济"的门.  

## 五、带缓冲级的 CMOS 门

**问题**: 与非门的输出电阻随输入状态变化(并联导通时电阻小, 单管导通时电阻大), 导致电压传输特性的转折电压偏离 $\frac{V_{DD}}{2}$, 噪声容限下降; 且输入数越多, 特性越差.

**解决**: 在基本门的输出端(或输入, 输出端各) 加一级反相器缓冲, 得到"带缓冲的 CMOS 门", 如 4000 系列的 B 系列(如 CD4011B).

| 对比项 | 无缓冲门 | 带缓冲门 |
| ------ | -------- | -------- |
| 传输特性 | 随输入状态变化 | 稳定, 一致 |
| 噪声容限 | 随输入状态变化 | 恒定 |
| 增益 | 较低 | 高 |
| 传输延迟 | 较小 | 略大(多一级) |

**代价**: 多一级反相器使传输延迟增加, 且输出逻辑功能需要重新确认(加缓冲不改变逻辑功能, 只改善特性).

## 六、输入端扩展的注意点

- 输入端数越多, 串联管的等效导通电阻越大, 延迟越大.  
- 因此多输入(如 8 输入) 的与非门通常用**多级门级联**实现, 而不是单级串联 8 个 NMOS.  
- 未使用的输入端必须按"与门接高, 或门接低"的原则处理, 不能悬空.  

## 七、OD 门(漏极开路门)

把输出级的 PMOS 去掉, 只保留下拉 NMOS, 就得到**漏极开路门(OD 门)**, 功能与 TTL 的 OC 门对应: 必须外接上拉电阻, 可实现**线与**和电平转换.

## 易错点

- 常有人把 CMOS 与非门画成"PMOS 串, NMOS 并", 这是错的; 正确组合是 NMOS 串对 PMOS 并.  
- CMOS 基本门输出的是反相结果, 画逻辑图时不要漏掉输出级的隐含反相.  

## 相关笔记

- 前置:[CMOS反相器](./CMOS反相器.md)  
- 由传输门构成的特殊结构:[CMOS传输门](./CMOS传输门.md)  
- 线与的实现:[OC门与三态门](./OC门与三态门.md)  
