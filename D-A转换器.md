---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第十一章 数-模和模-数转换 / 11.2 D/A转换器

# D-A转换器

## 核心结论

D/A 转换的基本思想是:**用数字量的各位控制对应权重的电流(或电压), 再求和**. 权电阻网络直观但阻值范围过大;**倒 T 型 R-2R 网络**只需两种阻值, 且各支路电流恒定, 是实际器件的标准结构.

## 一、权电阻网络 D/A 转换器

**结构**: 一个求和运算放大器, 输入端接 $n$ 条支路, 第 $i$ 位的支路电阻为 $2^{n-1-i}R$(即权重越高的位电阻越小), 由数字位 $d_i$ 控制该支路是接 $V_{REF}$ 还是接地.

**输出公式**:

$$v_O = -\frac{R_F}{R} \cdot V_{REF} \cdot \frac{1}{2^{n-1}} \sum_{i=0}^{n-1} d_i 2^i$$

或写成以 $D$ 表示数字量($D = \sum d_i 2^i$):

$$v_O = -\frac{R_F}{R} \cdot V_{REF} \cdot \frac{D}{2^{n-1}}$$

**优点**: 原理直观, 与各权重一一对应.

**缺点**(致命):

1. 阻值范围大:$n$ 位需要 $R$ 至 $2^{n-1}R$ 的电阻, 8 位时最大与最小相差 128 倍, 12 位时相差 2048 倍.  
2. 精度难以保证: 大阻值电阻的精度与温度特性都难以匹配, 集成电路工艺尤其困难.  

因此权电阻网络只适合低位数(4 位左右) 的场合.

## 二、倒 T 型 R-2R 电阻网络 D/A 转换器

**结构**:

- 电阻网络只有两种阻值:$R$ 与 $2R$, 构成"倒 T"形状.  
- 每个节点向右, 向下看进去的等效电阻都是 $R$, 因此从参考源流入的电流每经过一个节点就**二等分**.  
- 各支路的开关把电流切换到运放的**虚地端**(输出为 0) 或地.  

**关键优势**:

1. **只需两种阻值**, 易于集成且精度高.  
2. **各支路电流恒定**: 因为开关总是把电流接到虚地或地(两者电位相同), 支路电流与开关状态无关, 因此**开关导通电阻不影响精度**.  
3. 转换速度快: 不存在权电阻网络中大电阻的 RC 延迟.  

**输出公式**:

$$v_O = -\frac{R_F}{R} \cdot V_{REF} \cdot \frac{D}{2^n}$$

其中 $D$ 是 $n$ 位数字量的十进制值,$R_F$ 是反馈电阻.

**示例**:$n = 8$,$V_{REF} = 5\ \text{V}$,$R_F = R$,$D = 128$(即 10000000) 时

$$v_O = -5 \times \frac{128}{256} = -2.5\ \text{V}$$

## 三、R-2R 梯形(正 T 型) 网络

与倒 T 型对称: 把 $2R$ 与 $R$ 的位置互换, 工作原理类似, 但各支路电流会随开关位置变化, 对开关导通电阻较敏感, 性能略逊于倒 T 型.

## 四、D/A 转换器的分辨率

$$1\ \text{LSB} = \frac{V_{REF}}{2^n} \quad (\text{当 } R_F = R)$$

**提高分辨率的方式**:

1. 增加位数 $n$.  
2. 减小参考电压(但会同时减小输出范围).  

**注意**: 分辨率只取决于位数与参考电压, 与实际转换误差无关.

## 五、转换误差

| 误差 | 表现 | 可否校准 |
| ---- | ---- | -------- |
| 失调误差 | 输入全 0 时输出不为 0 | 可(调运放失调) |
| 增益误差 | 满量程输出偏离理想值 | 可(调 $R_F$ 或 $V_{REF}$) |
| 微分非线性 DNL | 相邻码的输出增量偏离 1 LSB | 不可 |
| 积分非线性 INL | 实际曲线偏离理想直线的最大值 | 不可 |
| 温度漂移 | 上述参数随温度变化 | 部分可补偿 |

**单调性**是 D/A 的重要指标: 若输入数字量增大时输出反而减小, 称为"非单调", 在闭环控制中会引起振荡.**DNL 小于 1 LSB 是保证单调性的充分条件**.

## 六、建立时间

**定义**: 从数字输入发生变化, 到输出模拟量达到并保持在终值 ±1/2 LSB 范围内所需的时间.

**组成**:

1. 开关切换时间.  
2. 运算放大器的压摆率(slew rate) 限制.  
3. 输出端的 RC 建立过程.  

**典型值**: 高速 D/A 可达数十纳秒, 通用型为微秒级.

## 七、输出形式

| 形式 | 说明 |
| ---- | ---- |
| 电压输出 | 内部已含输出放大器, 直接输出电压 |
| 电流输出 | 输出与数字量成正比的电流, 需外接运放转成电压(如 DAC0832) |
| 单极性 | 输出范围如 0 至 5 V |
| 双极性 | 输出范围如 -5 V 至 +5 V(需加偏置电路或偏移二进制码) |

## 八、典型芯片

| 型号 | 位数 | 特点 |
| ---- | ---- | ---- |
| DAC0832 | 8 位 | 电流输出, 需外接运放, 经典教学用芯片 |
| DAC1210 | 12 位 | 电流输出 |
| DAC7512 | 12 位 | 电压输出, SPI 接口 |
| MCP4725 | 12 位 | 电压输出, I2C 接口, 内置 EEPROM |
| AD5662 | 16 位 | 高精度, SPI 接口 |

## 九、应用

1. **波形发生器**: 把周期波形的采样值依次送出, 经 D/A 与低通滤波得到模拟波形(DDS 技术的核心).  
2. **数控电源/数控增益**: 用数字量控制输出电压或增益.  
3. **音频回放**: 音频 DAC 是 D/A 最常见的消费级应用.  
4. **闭环控制**: 把数字控制量变成模拟驱动量.  
5. **A/D 转换器的内部构件**: 逐次逼近型 ADC 内部必须有一个 D/A 转换器.  

## 十、使用要点

1. **参考电压必须稳定**:$V_{REF}$ 的精度直接决定输出精度, 应使用专用基准源.  
2. **输出要加滤波**: 阶梯状输出需经低通滤波平滑(重建滤波器).  
3. **注意建立时间**: 送入新数据的速率不能超过建立时间允许的范围.  
4. **电流输出型要配运放**: 注意运放的压摆率与带宽要满足建立时间要求.  

## 易错点

- 认为倒 T 型的输出公式分母是 $2^{n-1}$: 倒 T 型为 $2^n$, 权电阻网络为 $2^{n-1}$, 两者不同.  
- 认为位数越多输出越准: 位数只决定分辨率, 实际精度还取决于参考电压, 运放与非线性误差.  
- 忽略运放的负号: 上述公式中的负号来自反相求和结构, 实际电路中常再加一级反相器取正.  

## 相关笔记

- 前置:[数模与模数转换概述](./数模与模数转换概述.md)  
- 逆过程:[采样定理与量化编码](./采样定理与量化编码.md)  
- D/A 在 ADC 中的应用:[逐次逼近型与并联比较型ADC](./逐次逼近型与并联比较型ADC.md)  
