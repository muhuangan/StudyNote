---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第四章 组合逻辑电路 / 4.3 设计方法

# 组合逻辑电路的设计方法

## 核心结论

设计流程是分析的逆过程:**逻辑抽象 → 列真值表 → 写函数式并化简 → 画逻辑图**. 其中最关键也最容易出错的是第一步—把文字描述的需求变成"输入变量, 输出变量, 取值含义"的严格约定.

## 一、设计步骤

1. **逻辑抽象**: 确定输入变量, 输出变量的个数与含义, 规定 0 和 1 分别代表什么状态.  
2. **列真值表**: 枚举全部输入组合, 根据功能要求填写输出(含无关项时填 $\times$).  
3. **写出逻辑函数式并化简**: 由真值表写最小项之和, 再用公式法或卡诺图化简.  
4. **变换形式**: 根据可用器件把表达式变换为与非-与非式, 或非-或非式等形式.  
5. **画逻辑图**: 按表达式用门电路(或 MSI 器件) 实现.  

## 二、逻辑抽象的关键性

**必须明确的三件事**:

1. 有几个输入, 几个输出.  
2. 每个变量的**取值含义**(0 表示什么, 1 表示什么).  
3. 哪些输入组合**不会出现**(无关项).  

**示例**: 设计一个"三变量多数表决电路"—三人投票, 两人以上同意则通过.

- 输入:$A$,$B$,$C$, 取 1 表示同意, 0 表示反对.  
- 输出:$F$, 取 1 表示通过, 0 表示否决.  
- 无关项: 无.  

如果反过来把"0 表示同意", 则最终表达式完全不同, 但功能等价. 因此**约定必须先于真值表**.

## 三、完整设计示例

**题目**: 设计一个 4 位二进制数 $ABCD$ 的判别电路, 当该数大于等于 5 且小于等于 10 时输出 1.

**第一步: 真值表**. 输入为 4 位二进制数($A$ 为最高位), 输出 $F$.

**第二步: 函数式**.

$$F = \sum m(5, 6, 7, 8, 9, 10)$$

**第三步: 卡诺图化简**(AB 为行, CD 为列).

| AB \ CD | 00 | 01 | 11 | 10 |
| ------- | -- | -- | -- | -- |
| 00 | 0 | 0 | 0 | 0 |
| 01 | 0 | 1 | 1 | 1 |
| 11 | 0 | 0 | 0 | 0 |
| 10 | 1 | 1 | 0 | 1 |

圈组(共 4 组, 均为 2 格圈,$m_7$ 与 $m_8$ 各被两个圈共用):

- 组一:$m_5(0101)$ 与 $m_7(0111)$, 只有 C 不同, 得 $\overline{A}BD$.  
- 组二:$m_6(0110)$ 与 $m_7(0111)$, 只有 D 不同, 得 $\overline{A}BC$.  
- 组三:$m_8(1000)$ 与 $m_9(1001)$, 只有 D 不同, 得 $A\overline{B}\,\overline{C}$.  
- 组四:$m_8(1000)$ 与 $m_{10}(1010)$, 只有 C 不同, 得 $A\overline{B}\,\overline{D}$.  

**说明**:$m_5$,$m_6$,$m_7$ 三格不是 $2^k$ 个, 不能成圈, 只能两两成圈并共用 $m_7$;$m_8$,$m_9$,$m_{10}$ 同理共用 $m_8$. 共用方格是允许的, 但每个圈必须含有其他圈未覆盖的新格—此处四个圈分别独有 $m_5$,$m_6$,$m_9$,$m_{10}$, 无冗余圈.

**第四步: 合并同类项**.

$$F = \overline{A}BD + \overline{A}BC + A\overline{B}\,\overline{C} + A\overline{B}\,\overline{D} = \overline{A}B(C + D) + A\overline{B}\,\overline{CD}$$

**验证**: 代入 $m_5(0101)$,$\overline{A}B(C+D) = 1 \cdot 1 \cdot (0+1) = 1$, 输出 1; 代入 $m_{11}(1011)$,$A\overline{B}\,\overline{CD} = 1 \cdot 1 \cdot \overline{1 \cdot 1} = 0$, 且第一项为 0, 输出 0. 均与真值表一致.

**第四步: 变换为与非-与非式**(若只有与非门可用), 对整体两次取反即可.

**第五步: 画逻辑图**, 按表达式用门实现.

## 四、MSI 器件的设计路线

当变量较多或要求器件数量少时, 不直接搭门, 而是用功能模块实现:

| 可用器件 | 实现思路 |
| -------- | -------- |
| 译码器 + 与非门 | 变量作地址, 把函数包含的最小项对应的输出接入与非门 |
| 数据选择器 | 部分变量作地址, 其余变量作数据输入(降维法) |
| 加法器 | 实现算术类函数 |
| 比较器 | 实现大小判断类函数 |
| ROM / PLD | 直接把真值表烧录进去 |

详见"用中规模器件实现逻辑函数".

## 五、设计中的工程约束

| 约束 | 应对 |
| ---- | ---- |
| 只有单一类型的门 | 变换表达式形式 |
| 输入端数受限(如只有 2 输入门) | 分解多输入运算 |
| 级数受限(速度要求) | 优先减少级数而非门数 |
| 输出毛刺敏感 | 检查竞争-冒险并消除 |
| 成本受限 | 优先用 MSI 功能模块 |

## 六、设计检查清单

1. 真值表是否覆盖了全部输入组合?  
2. 无关项是否已标出并合理使用?  
3. 化简结果是否最简(用卡诺图验证过)?  
4. 表达式形式是否与可用器件匹配?  
5. 是否检查了竞争-冒险?  
6. 扇出, 扇入是否满足器件参数?  

## 易错点

- 跳过逻辑抽象直接列真值表, 导致 0/1 含义混乱, 结果南辕北辙.  
- 忘记标注无关项, 白白损失化简机会.  
- 设计完成时忽略竞争-冒险检查, 电路在实验室能工作但批量生产时出现毛刺故障.  

## 相关笔记

- 前置:[组合逻辑电路概述](./组合逻辑电路概述.md)  
- 化简工具:[卡诺图化简法](./卡诺图化简法.md),[无关项与逻辑函数化简](./无关项与逻辑函数化简.md)  
- 形式变换:[逻辑函数形式的变换](./逻辑函数形式的变换.md)  
- 后续检查:[竞争冒险现象](./竞争冒险现象.md)  
