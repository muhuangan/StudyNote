---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第十一章 数-模和模-数转换 / 11.3 采样、保持、量化与编码

# 采样定理与量化编码

## 核心结论

A/D 转换分四步:**采样(时间离散化)→ 保持(维持稳定)→ 量化(幅值离散化)→ 编码(变二进制)**. 采样必须满足**采样定理**($f_s \ge 2f_m$), 否则产生混叠; 量化必然引入**量化误差**, 这是 A/D 的固有误差, 只能靠增加位数减小.

## 一、采样定理(奈奎斯特-香农定理)

**内容**: 为了能从采样信号中无失真地恢复原信号, 采样频率 $f_s$ 必须不小于原信号中最高频率分量 $f_m$ 的 2 倍:

$$f_s \ge 2 f_m$$

$f_s / 2$ 称为**奈奎斯特频率**.

**混叠(Aliasing)**: 若 $f_s < 2f_m$, 高频分量会"折叠"到低频区域, 产生原信号中不存在的低频成分.

$$f_{混叠} = |f_{信号} - k \cdot f_s| \quad (k \text{ 为整数})$$

**示例**: 用 10 kHz 采样一个 7 kHz 的正弦波, 混叠频率为 $|7 - 10| = 3\ \text{kHz}$, 采样结果看起来是 3 kHz 的信号, 完全错误.

**工程对策**:

1. **抗混叠滤波器**: 在采样前用低通滤波器把高于 $f_s / 2$ 的分量滤掉(这是必须的, 不能省略).
2. **提高采样率**: 实际常取 $f_s = (3 \sim 10) f_m$, 留出滤波器过渡带的余量.

**常见采样率举例**:

| 应用       | 采样率          | 信号带宽       |
| ---------- | --------------- | -------------- |
| 电话语音   | 8 kHz           | 约 3.4 kHz     |
| CD 音频    | 44.1 kHz        | 20 kHz         |
| 高解析音频 | 96 / 192 kHz    | 约 48 / 96 kHz |
| 超声波测距 | 数十 kHz 至 MHz | 视探头而定     |

## 二、采样保持电路

**为什么需要保持**: A/D 转换需要一定时间(转换时间), 在此期间若输入电压还在变化, 转换结果就会"不知所云". 采样保持电路先把电压"冻结", 再交给 ADC 转换.

**结构**: 一个模拟开关 + 一个保持电容 $C_H$ + 一个电压跟随器.

| 阶段 | 开关 | 电容             | 输出               |
| ---- | ---- | ---------------- | ------------------ |
| 采样 | 导通 | 跟随输入电压充电 | 跟随输入           |
| 保持 | 断开 | 电压保持不变     | 保持采样瞬间的电压 |

**关键参数**:

| 参数               | 含义                                                   |
| ------------------ | ------------------------------------------------------ |
| 孔径时间 $t_{ap}$  | 从发出保持命令到开关真正断开的延迟, 会引入时间不确定度 |
| 捕捉时间 $t_{ac}$  | 从保持切换到采样后, 电容充电到规定精度所需的时间       |
| 下垂率(droop rate) | 保持期间因漏电流导致的电压下降速率                     |

**孔径误差**: 若输入信号变化率为 $dv/dt$, 孔径时间为 $t_{ap}$, 则孔径误差约为 $\dfrac{dv}{dt} \cdot t_{ap}$. 信号频率越高, 孔径误差越严重, 这也是高速 ADC 对采样保持电路要求极高的原因.

## 三、量化

**定义**: 把采样得到的连续幅值归入有限个离散电平之一.

**两种量化方式**:

| 方式           | 规则                   | 最大量化误差 |
| -------------- | ---------------------- | ------------ |
| 只舍不入(截断) | 归入不超过它的最大电平 | 1 LSB        |
| **四舍五入**   | 归入最接近的电平       | **1/2 LSB**  |

实际 ADC 普遍采用**四舍五入**方式, 因为其误差更小且关于零点对称.

**量化单位(量化间隔)**:

$$\Delta = \frac{V_{FSR}}{2^n} = 1\ \text{LSB}$$

**量化误差**:

$$\varepsilon_{max} = \pm \frac{1}{2}\ \text{LSB}$$

**重要结论**: 量化误差是**固有**的, 不可消除的, 只能通过增加位数(减小 $\Delta$) 来减小; 它表现为叠加在信号上的量化噪声.

**量化信噪比**(理想 $n$ 位, 满量程正弦输入):

$$\text{SNR} \approx 6.02 n + 1.76\ \text{dB}$$

即每增加 1 位, 信噪比提高约 6 dB. 这是"位数决定动态范围"的定量依据.

## 四、编码

**定义**: 把量化后的离散电平用二进制代码表示.

**常见编码方式**:

| 编码         | 特点                                                    |
| ------------ | ------------------------------------------------------- |
| 自然二进制码 | 最直观, 全 0 对应 0 V                                   |
| 偏移二进制码 | 把 0 V 编码在中间(如 8 位时的 10000000), 用于双极性信号 |
| 补码         | 与有符号数的补码表示一致, 便于运算                      |
| 格雷码       | 相邻电平只差一位, 用于减少编码边界处的误码              |

**双极性信号的编码约定**:

| 输入范围 | 偏移二进制(8 位) |
| -------- | ---------------- |
| 负满量程 | 00000000         |
| 零       | 10000000         |
| 正满量程 | 11111111         |

## 五、完整的 A/D 过程示例

设 3 位 ADC, 输入范围 0 至 8 V, 采用四舍五入量化.

$$\Delta = \frac{8}{2^3} = 1\ \text{V}$$

| 输入电压     | 量化电平 | 二进制输出 |
| ------------ | -------- | ---------- |
| 0.0 至 0.5 V | 0 V      | 000        |
| 0.5 至 1.5 V | 1 V      | 001        |
| 1.5 至 2.5 V | 2 V      | 010        |
| 2.5 至 3.5 V | 3 V      | 011        |
| 3.5 至 4.5 V | 4 V      | 100        |
| 4.5 至 5.5 V | 5 V      | 101        |
| 5.5 至 6.5 V | 6 V      | 110        |
| 6.5 至 8.0 V | 7 V      | 111        |

**注意**: 最大输出 111 对应 7 V 而不是 8 V, 这是 $n$ 位编码只能表示 $2^n$ 个电平而满量程被分成 $2^n$ 个区间的结果.

## 六、采样保持与孔径的系统影响

| 影响因素       | 后果               | 对策                             |
| -------------- | ------------------ | -------------------------------- |
| 采样率不足     | 混叠               | 抗混叠滤波器 + 提高采样率        |
| 孔径时间不确定 | 高频信号采样值不准 | 选用低孔径抖动的采样保持器       |
| 电容漏电       | 保持期间电压下垂   | 选用低漏电电容(聚丙烯, 聚苯乙烯) |
| 捕捉时间不足   | 采样值未达精度     | 延长采样阶段时间                 |

## 易错点

- 认为"采样率等于信号最高频率的 2 倍就够了": 这只是理论下限, 工程上必须留过渡带余量并配抗混叠滤波器.
- 认为量化误差可以通过改进电路消除: 量化误差是原理性误差, 只要位数有限就必然存在.
- 忽略孔径时间: 在高频信号采集中, 孔径抖动往往是限制有效位数的主要因素, 而不是 ADC 本身的位数.

## 相关笔记

- 前置:[数模与模数转换概述](./数模与模数转换概述.md)
- 各类 ADC:[逐次逼近型与并联比较型ADC](./逐次逼近型与并联比较型ADC.md),[双积分型与Σ-Δ型ADC](./双积分型与Σ-Δ型ADC.md)
- 数字信号的定义:[数字信号与模拟信号](./数字信号与模拟信号.md)
