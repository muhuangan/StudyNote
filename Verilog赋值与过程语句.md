---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第九章 硬件描述语言 / 9.2 assign, always 与阻塞/非阻塞赋值

# Verilog赋值与过程语句

## 核心结论

Verilog 的赋值分为**连续赋值 `assign`**(描述组合逻辑) 与**过程赋值**(在 `always`/`initial` 块内). 过程赋值又有**阻塞 `=`** 与**非阻塞 `<=`** 两种, 这是 Verilog 最关键, 最容易出错的概念:**组合逻辑用阻塞, 时序逻辑用非阻塞.**

## 一、连续赋值 assign

**语法**:

```verilog
assign 目标 = 表达式;
```

**特点**:

1. 目标必须是 **`wire`** 型.  
2. 只要右侧表达式的值变化, 左侧**立即**跟着变化—这就是"连续"的含义.  
3. 综合为**组合逻辑电路**.  
4. 位于 `always` 块之外, 与其他语句**并行**执行.  

```verilog
wire y;
assign y = (a & b) | c;      // 综合为一个与门加一个或门

wire [3:0] sum;
assign {cout, sum} = a + b;  // 拼接后赋值,cout 为进位
```

## 二、过程块 always

**语法**:

```verilog
always @(敏感表) begin
    语句;
end
```

**敏感表的两种写法**:

| 写法 | 含义 | 综合结果 |
| ---- | ---- | -------- |
| `always @(posedge clk)` | 时钟上升沿触发 | **时序逻辑**(触发器) |
| `always @(negedge clk)` | 时钟下降沿触发 | 时序逻辑 |
| `always @(*)` 或 `always @*` | 所有输入信号变化都触发 | **组合逻辑** |
| `always @(a or b)` | 显式列出敏感信号 | 组合逻辑(不推荐, 易漏) |

**要点**:

1. `always` 块中被赋值的信号必须是 **`reg`** 型.  
2. `always` 块是**无限循环**的: 仿真时从时间 0 开始反复执行(每当敏感表事件发生时执行一次).  
3. 敏感表中若漏掉某个信号, 仿真结果会与综合后的电路不符(前仿真正确, 后仿真错误), 因此组合逻辑一律用 `always @(*)`.  

## 三、阻塞赋值 `=`

**语义**:**立即**完成赋值, 当前语句执行完后变量的值已经改变, 后续语句看到的是新值.

```verilog
always @(*) begin
    temp = a & b;     // temp 立即被赋值
    y    = temp | c;  // 使用的是刚才更新过的 temp
end
```

**行为特点**: 类似 C 语言的顺序执行, 语句之间有先后依赖关系.

**用途**: 描述**组合逻辑**.

## 四、非阻塞赋值 `<=`

**语义**: 所有右侧表达式**同时求值**, 赋值在**当前块结束时统一完成**. 语句之间不存在先后依赖.

```verilog
always @(posedge clk) begin
    q1 <= din;    // 与下一行同时求值
    q2 <= q1;     // 用的是 q1 的旧值(上升沿之前的值)
end
```

**关键效果**: 上面这段代码综合出的是**两级移位寄存器**(q1 与 q2 同时更新, q2 得到的是 q1 的旧值). 若改用阻塞赋值 `=`, q2 会得到 q1 的新值, 两级被"短路"成一级—**这是初学者最常犯的错误**.

**用途**: 描述**时序逻辑**.

## 五、阻塞与非阻塞的对比

| 对比项 | 阻塞 `=` | 非阻塞 `<=` |
| ------ | -------- | ----------- |
| 生效时刻 | 立即 | 块结束时 |
| 语句间依赖 | 有(后句看到新值) | 无(都用旧值) |
| 综合结果 | 组合逻辑(在 `always @(*)` 中) | 触发器(在 `always @(posedge clk)` 中) |
| 适用场景 | 组合逻辑 | 时序逻辑 |
| 多寄存器级联 | 会"短路" | 正确产生多级流水线 |

**两条黄金规则**:

1. 在 `always @(posedge clk)` 块中描述时序逻辑,**一律用 `<=`**.  
2. 在 `always @(*)` 块中描述组合逻辑,**一律用 `=`**.  

**补充规则**:

- 不要在**同一个** `always` 块中混用两种赋值.  
- 不要对同一个变量在**多个** `always` 块中赋值(会产生多驱动冲突).  

## 六、条件语句 if / else

```verilog
always @(*) begin
    if (sel == 2'b00)
        y = a;
    else if (sel == 2'b01)
        y = b;
    else
        y = c;
end
```

**综合结果**: 多路选择器(MUX).

**重要陷阱**: 若 `if` 没有配套的 `else`, 且在组合逻辑块中使用, 会综合出**锁存器(Latch)**—因为"条件不满足时保持原值"正是锁存器的行为.

**避免意外锁存器的方法**:

1. 写完整的 `if...else`.  
2. 在进入分支前先赋默认值:

```verilog
always @(*) begin
    y = 1'b0;        // 默认赋值
    if (cond)
        y = a;
end
```

## 七、case 语句

```verilog
always @(*) begin
    case (sel)
        2'b00: y = a;
        2'b01: y = b;
        2'b10: y = c;
        2'b11: y = d;
        default: y = 1'b0;   // 必须有 default,否则产生锁存器
    endcase
end
```

**三种形式**:

| 形式 | 比较规则 |
| ---- | -------- |
| `case` | 逐位比较, 包括 x 与 z |
| `casez` | 把 `z` 当作不关心位 |
| `casex` | 把 `x` 与 `z` 都当作不关心位 |

`casez` 常用于优先级编码器等需要"不关心位"的场合:

```verilog
casez (irq)
    4'b1???: level = 3'd4;   // ? 表示该位不关心
    4'b01??: level = 3'd3;
    4'b001?: level = 3'd2;
    4'b0001: level = 3'd1;
    default: level = 3'd0;
endcase
```

**注意**:`casex` 在综合时可能导致前后仿真不一致, 工程上较少使用.

## 八、循环语句

| 语句 | 可综合性 |
| ---- | -------- |
| `for` | **可综合**(循环次数在编译时确定, 展开为重复硬件) |
| `while` | 一般不可综合 |
| `repeat` | 一般不可综合(仿真用) |
| `forever` | 不可综合(testbench 用) |

**可综合的 for 循环典型用法**:

```verilog
integer i;
always @(*) begin
    for (i = 0; i < 8; i = i + 1) begin
        out[i] = data[i] ^ mask[i];   // 展开为 8 个异或门
    end
end
```

## 九、begin / end 与 fork / join

| 结构 | 执行方式 | 用途 |
| ---- | -------- | ---- |
| `begin ... end` | **顺序**执行块内语句 | RTL 描述 |
| `fork ... join` | **并行**执行块内语句 | 仅用于 testbench, 不可综合 |

## 十、可综合子集的要点回顾

**推荐写法**:

```verilog
// 组合逻辑:assign + wire
wire y;
assign y = a & b;

// 组合逻辑:always @(*) + 阻塞赋值 + reg
reg y;
always @(*) begin
    y = a & b;
end

// 时序逻辑:always @(posedge clk) + 非阻塞赋值 + reg
reg q;
always @(posedge clk) begin
    q <= d;
end
```

**禁止写法**(RTL 中):

- `initial` 块(除测试平台)  
- `#` 延迟  
- `fork / join`  
- 未完整赋值的 `if` / `case`(会产生锁存器)  
- 对同一变量多角度赋值  

## 易错点

- 在时序逻辑中误用阻塞赋值: 仿真时可能"看起来正确"(因为顺序执行恰好匹配), 但综合后的电路与仿真不一致—这是最难排查的一类错误.  
- `case` 缺少 `default` 分支: 在组合逻辑中会综合出锁存器, 导致电路行为异常.  
- 敏感表写 `always @(a)` 而漏了 `b`: 仿真时 `b` 变化不会触发, 但综合后的电路会响应—前后仿真不一致.  

## 相关笔记

- 前置:[Verilog数据类型与运算符](./Verilog数据类型与运算符.md)  
- 组合逻辑实践:[Verilog组合逻辑描述](./Verilog组合逻辑描述.md)  
- 时序逻辑实践:[Verilog时序逻辑与状态机](./Verilog时序逻辑与状态机.md)  
