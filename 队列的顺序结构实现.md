---
created_at: 2026-03-20
updated_at: 2026-09-18
tags:
    - 计算机基础
archived: true
---

[数据结构](./数据结构.md) / 3. 栈和队列 / 3.2 队列定义与实现

# 队列的顺序结构实现

**队列**是一种**先进先出**(First In First Out, FIFO) 的线性表, 只允许在表的一端进行插入, 另一端进行删除

允许插入的一端叫**队尾**, 允许删除的一端叫**队头**


**结构定义**  
典型应用

```C
#define MAXSIZE 100
typedef int ElemType;
typedef struct{
    ElemType data[MAXSIZE];
    int front;
    int rear;
}Queue;
```

**初始化**  
典型应用

```C
void initQueue(Queue * Q){
    Q -> front = 0;
    Q -> rear = 0;
}
```

**判断队列是否为空**  
典型应用

```C
int isEmpty(Queue *Q){
    if(Q -> front == Q -> rear){
        printf("空的\n");
        return 1;
    }
    else{
        return 0;
    }
}
```

**出队**  
典型应用

```C
ElemType dequeue(Queue *Q){
    if(Q -> front == Q -> rear){
        printf("空的\n");
        return 0;
    }
    ElemType e = Q -> data[Q -> front];
    Q -> front++;
    return e;
}
```

**入队**  
典型应用

```C
int equeue(Queue *Q, ElemType e){
    if(Q -> rear >= MAXSIZE){
        if(!queueFull(Q)){
            return 0;
        }
    }
    Q -> data[Q -> rear] = e;
    Q -> rear++;
    return 1;
}

//即使rear指向最后队列也可能还没满, 这个函数用于挪动队列
int queueFull(Queue *Q){
    if(Q -> front > 0){
        int step = Q -> front;
        for(int i = Q -> front; i < Q -> rear; i++){
            Q -> data[i - step] = Q -> data[i];
        }
        Q -> front = 0;
        Q -> rear = Q -> rear - step;
        return 1;
    }
    else{
        printf("真的满了\n");
        return 0;
    }
}
```

不难看出想要充分利用顺序队列中的空间就不得不对所有元素进行挪动, 这无疑是一种低效的方法, 因此, 循环队列的重要性得以凸显

## 相关笔记

- 上一篇:[栈的链式结构实现](./栈的链式结构实现.md)  
- 下一篇:[循环队列的顺序结构实现](./循环队列的顺序结构实现.md)  
- 返回索引:[数据结构](./数据结构.md)  
