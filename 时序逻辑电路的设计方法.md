---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第六章 时序逻辑电路 / 6.4 设计方法(含 6.5 竞争-冒险)

# 时序逻辑电路的设计方法

## 核心结论

设计流程是:**逻辑抽象 → 状态图 → 状态化简 → 状态编码 → 选触发器 → 求驱动方程与输出方程 → 画逻辑图 → 自启动检查**. 其中"状态化简"决定电路的规模,"自启动检查"决定电路能否可靠上电工作.

## 一、设计步骤

1. **逻辑抽象**: 确定输入变量, 输出变量, 状态个数及各状态的含义.  
2. **画状态转换图**(或状态转换表).  
3. **状态化简**: 合并等价状态, 减少触发器个数与组合逻辑规模.  
4. **状态编码**: 给每个状态分配一组二进制代码.  
5. **选择触发器类型**: 通常选 D 触发器(激励最简单).  
6. **求驱动方程与输出方程**: 由编码后的状态表经卡诺图化简得到.  
7. **画逻辑图**.  
8. **自启动检查**: 验证无效状态能否进入有效循环.  

## 二、状态化简

**等价状态的判定**: 若两个状态在满足以下两个条件时, 则称它们等价:

1. 在**任意输入**下, 输出完全相同;  
2. 在任意输入下, 次态"等价"(次态相同, 或次态互为等价状态).  

等价状态可以合并为一个状态, 从而减少状态数.

**化简方法**:

| 方法 | 适用 |
| ---- | ---- |
| 观察法 | 状态少(小于 8) 时直接比较 |
| 隐含表法 | 状态多时系统化求解 |

**隐含表法要点**: 把所有状态两两配对填入三角形表格, 若两状态输出不同则打叉(不等价); 若输出相同则把它们的次态对填入格中, 检查这些次态对是否已被判为不等价, 若是则本对也不等价. 反复迭代直到不再变化.

**注意**: 状态化简只在"完全描述的"状态机(所有输入组合都有定义) 中严格成立; 含无关项时需谨慎处理.

## 三、状态编码

**编码位数**:$M$ 个状态需要 $n$ 位编码, 满足

$$2^{n-1} < M \le 2^n$$

**三种常用编码方案**:

| 方案 | 特点 | 适用 |
| ---- | ---- | ---- |
| 二进制编码(顺序二进制) | 编码最紧凑, 触发器最少 | 通用 |
| 格雷码 | 相邻状态只有一位变化, 竞争-冒险最少 | 状态按序转移(如计数器) |
| 一位热码(One-Hot) | $M$ 个状态用 $M$ 个触发器, 每个状态只有一位为 1 | **FPGA 首选** |

**编码方案影响电路复杂度**: 不同的编码会得到不同的驱动方程复杂度. 理论上需要尝试多种方案择优, 实践中按上表选择即可.

**One-Hot 编码的优势**(FPGA 场景):

1. 次态逻辑只需检查"哪一位为 1", 组合逻辑简单, 速度快.  
2. 状态译码无需译码器, 直接用某一位即为该状态的标志.  
3. FPGA 中触发器资源丰富而组合逻辑资源相对紧张, 用触发器换速度是划算的.  

## 四、完整设计示例: 111 序列检测器

**题目**: 设计一个时序电路, 检测串行输入 $X$ 中是否出现连续的三个(或以上)1. 出现时输出 $Y = 1$, 否则 $Y = 0$.

**第一步: 逻辑抽象**.

- 输入 $X$: 串行数据.  
- 输出 $Y$: 检测结果.  
- 状态:$S_0$(尚未收到 1),$S_1$(已收到 1 个连续 1),$S_2$(已收到 2 个连续 1).  

**第二步: 状态转换图**(Mealy 型).

| 现态 | $X = 0$ | $X = 1$ |
| ---- | ------- | ------- |
| $S_0$ | $S_0$ / 0 | $S_1$ / 0 |
| $S_1$ | $S_0$ / 0 | $S_2$ / 0 |
| $S_2$ | $S_0$ / 0 | $S_2$ / 1 |

(表中"状态 / 输出")

**第三步: 状态化简**.$S_0$,$S_1$,$S_2$ 输出与次态均不同, 无等价状态, 无需化简.

**第四步: 状态编码**.3 个状态需 2 位编码, 取

$$S_0 = 00,\quad S_1 = 01,\quad S_2 = 10$$

状态 11 为无效状态(作无关项处理).

**第五步: 选触发器**. 选两个上升沿 D 触发器, 激励 $D_1$,$D_0$ 直接等于次态 $Q_1^*$,$Q_0^*$.

**第六步: 状态表与驱动方程**.

| $Q_1 Q_0$ | $X$ | $Q_1^* Q_0^*$ | $D_1$ | $D_0$ | $Y$ |
| --------- | --- | ------------- | ----- | ----- | --- |
| 00 | 0 | 00 | 0 | 0 | 0 |
| 00 | 1 | 01 | 0 | 1 | 0 |
| 01 | 0 | 00 | 0 | 0 | 0 |
| 01 | 1 | 10 | 1 | 0 | 0 |
| 10 | 0 | 00 | 0 | 0 | 0 |
| 10 | 1 | 10 | 1 | 0 | 1 |
| 11 | 0 | $\times$ | $\times$ | $\times$ | $\times$ |
| 11 | 1 | $\times$ | $\times$ | $\times$ | $\times$ |

**卡诺图化简**(以 $Q_1 Q_0 X$ 为变量). 把无效状态 11 的次态按"有利于自启动"赋值:$11 \xrightarrow{X=0} 00$,$11 \xrightarrow{X=1} 10$, 则

$$D_1 = X(Q_1 + Q_0)$$

$$D_0 = X\,\overline{Q_1}\,\overline{Q_0}$$

$$Y = X Q_1$$

**第七步: 画逻辑图**. 两个 D 触发器, 外加 2 个与门, 1 个或门, 2 个反相器.

**第八步: 自启动检查**. 无效状态 11:$X = 0$ 时次态为 00,$X = 1$ 时次态为 10, 均能进入有效循环(00 或 10), 因此**电路能自启动**.

## 五、自启动检查与处理

**检查方法**: 把无效状态代入驱动方程, 看其转移路径是否最终进入有效循环.

**若不能自启动, 三种补救措施**:

1. **重新分配无关项的取值**: 把无效状态的次态直接指向有效状态(如上例), 这是最经济的方法.  
2. **加异步复位电路**: 上电时把所有触发器置为有效初态.  
3. **改造组合逻辑**: 增加额外的转移通路, 代价是逻辑变复杂.  

## 六、时序电路中的竞争-冒险

与组合电路类似, 时序电路中也可能出现竞争-冒险, 但**表现与危害不同**:

| 层面 | 表现 | 影响 |
| ---- | ---- | ---- |
| 组合逻辑部分(次态逻辑) | 输出端有毛刺 | 若毛刺被触发器在时钟沿采样到, 会导致状态错误 |
| 时钟信号 | 时钟偏移(skew) | 触发器不同时更新, 产生类似异步电路的问题 |
| 异步输入端 | 建立/保持时间违规 | 触发器进入亚稳态 |

**消除方法**:

1. 毛刺通常出现在两次时钟沿之间, 只要触发器在时钟沿采样时毛刺已消失, 就不会影响结果—这是**同步设计天然免疫毛刺**的原因.  
2. 若毛刺恰好落在建立/保持窗口内, 需按组合电路的方法(加冗余项等) 消除.  
3. 时钟偏移通过合理的时钟树设计(FPGA 中用全局时钟资源) 来控制.  

## 七、设计检查清单

1. 状态图是否完整覆盖了所有输入组合?  
2. 状态是否已化简到最少?  
3. 编码方案是否合适(是否考虑过格雷码或 One-Hot)?  
4. 无关项是否已用于简化驱动方程?  
5. 是否检查了自启动?  
6. 输出是 Mealy 型还是 Moore 型, 与需求是否一致?  
7. 触发器的建立/保持时间是否满足?  

## 易错点

- 忘记检查自启动: 这是设计题中最常见的失分点, 无效状态若形成死循环, 电路上电后无法正常工作.  
- 编码后没有重新列状态表, 直接用文字状态名去化简, 导致方程错误.  
- 把 Mealy 与 Moore 的输出方程写错: Mealy 的输出与输入有关, Moore 的只与状态有关.  

## 相关笔记

- 前置:[时序逻辑电路的分析方法](./时序逻辑电路的分析方法.md)  
- 激励表工具:[触发器逻辑功能的描述](./触发器逻辑功能的描述.md)  
- 化简工具:[卡诺图化简法](./卡诺图化简法.md)  
- HDL 实现:[Verilog时序逻辑与状态机](./Verilog时序逻辑与状态机.md)  
