---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第四章 组合逻辑电路 / 4.4.2 显示译码器

# 显示译码器

## 核心结论

显示译码器把 BCD 码转换成驱动七段数码管的七位段码. 选型的唯一依据是**数码管的极性**: 共阴极配 74LS48(高电平有效输出), 共阳极配 74LS47(低电平有效输出).

## 一、七段数码管

**结构**: 七个发光二极管(LED) 排列成"日"字形, 分别记为 a, b, c, d, e, f, g, 部分器件还带小数点 dp.

**段位编号约定**(标准):

```
 aaa
f   b
f   b
 ggg
e   c
e   c
 ddd
```

**两种极性**:

| 类型 | 内部连接 | 驱动方式 | 配用芯片 |
| ---- | -------- | -------- | -------- |
| 共阴极(CC) | 全部 LED 阴极相连后接地 | 段引脚给**高电平**点亮 | 74LS48, CD4511 |
| 共阳极(CA) | 全部 LED 阳极相连后接电源 | 段引脚给**低电平**点亮 | 74LS47,74LS247 |

**限流电阻**: 每段必须串联限流电阻(通常 100 至 470 欧), 否则会烧毁 LED 或超出芯片驱动能力.

## 二、七段显示字形与段码

| 显示字符 | 点亮段 | 共阴极段码(abcdefg) |
| -------- | ------ | --------------------- |
| 0 | a b c d e f | 1111110 |
| 1 | b c | 0110000 |
| 2 | a b d e g | 1101101 |
| 3 | a b c d g | 1111001 |
| 4 | b c f g | 0110011 |
| 5 | a c d f g | 1011011 |
| 6 | a c d e f g | 1011111 |
| 7 | a b c | 1110000 |
| 8 | a b c d e f g | 1111111 |
| 9 | a b c d f g | 1111011 |

共阳极段码与共阴极段码**逐位相反**(按位取反).

## 三、74LS48(共阴极驱动、输出高电平有效)

**输入**: 4 位 8421 BCD 码 $A_3 A_2 A_1 A_0$($A_3$ 为高位).

**输出**:$a$ 至 $g$, 高电平有效, 内部含上拉电阻, 可直接驱动共阴极数码管.

**三个控制端**(低电平有效, 符号带横线):

| 控制端 | 名称 | 功能 |
| ------ | ---- | ---- |
| $\overline{LT}$ | 灯测试输入 | 为 0 时强制全部段点亮, 用于检查数码管是否损坏 |
| $\overline{RBI}$ | 灭零输入 | 为 0 且输入为 0000 时, 该位不显示(消隐无效零) |
| $\overline{BI}/\overline{RBO}$ | 灭灯输入 / 灭零输出 | 作输入时为 0 则全部熄灭; 作输出时为 0 表示本位正在进行灭零 |

**灭零的级联用法**: 在多位显示中, 把高位的 $\overline{RBO}$ 接到低位的 $\overline{RBI}$, 可以实现"整数部分前导零自动隐藏": 例如数值 0037 显示为 " 37", 只有当前位被灭零且本位也是 0 时, 才继续向下一位传递灭零信号.

**伪码处理**: 74LS48 对输入 1010 至 1111 有规定的显示图形(不是空白), 便于故障排查.

## 四、74LS47(共阳极驱动、输出低电平有效)

与 74LS48 功能相同, 引脚兼容, 但输出为**低电平有效**(集电极开路输出, 需外接上拉电阻), 驱动共阳极数码管. 其段码是 74LS48 段码的按位取反.

## 五、CD4511(CMOS 共阴极显示译码器)

除驱动能力较弱外, 功能与 74LS48 类似, 增加了一个**锁存端** $\overline{LE}$: 当 $\overline{LE} = 1$ 时锁存当前输入, 输出保持不变. 这一特性使其可以直接用于**动态扫描显示**—锁存后 CPU 可以去做别的事, 显示内容保持不变.

## 六、多位显示: 静态与动态

**静态显示**: 每一位用一片显示译码器, 同时点亮. 优点是亮度高, 无闪烁, 编程简单; 缺点是器件多, 功耗大.

**动态扫描显示**:

1. 多位数码管的同名段并联, 共用一组段码线.  
2. 用位选信号(由译码器或三态驱动器产生) 轮流选通每一位的公共端.  
3. 扫描频率高于 50 Hz 时, 人眼的视觉暂留效应使显示看起来是连续的.  

**关键参数**: 每位点亮时间占空比为 $1/N$($N$ 为位数), 因此瞬时电流要相应增大以保证平均亮度; 同时扫描频率要足够高以避免闪烁, 也要足够低以保证每位有足够的导通时间.

## 七、常见故障排查

| 现象 | 可能原因 |
| ---- | -------- |
| 全部段都不亮 | 公共端未接, 限流电阻开路, 驱动极性选错 |
| 某些段始终不亮 | 对应 LED 损坏, 译码器对应引脚虚焊 |
| 显示乱码 | BCD 输入顺序接反(低位当高位) |
| 亮度不均 | 动态扫描占空比不一致 |
| 前导零无法显示 | $\overline{RBI}$ 未正确处理 |

## 易错点

- 共阴极与共阳极**不能互换**, 接反会导致全不亮或全亮(反向耐压不足还可能损坏).  
- 段码表必须区分极性: 74LS48 的段码与 74LS47 的段码是逐位互补的.  

## 相关笔记

- 通用译码器原理:[译码器](./译码器.md)  
- BCD 码约定:[BCD码](./BCD码.md)  
- 动态扫描的时序基础:[时序逻辑电路概述](./时序逻辑电路概述.md)  
