---
created_at: 2026-09-17
updated_at: 2026-09-18
tags:
    - 数电
    - 电子电路基础
archived: false
---

[数电](./数电.md) / 第三章 门电路 / 3.3 CMOS门电路 / 电气特性与使用注意事项

# CMOS门电路电气特性

## 核心结论

CMOS 的电气特性可以浓缩成四条:**输入不取电流, 输入端必须不悬空, 静态几乎不耗电, 功耗随频率线性增长**. 理解这四条即可处理绝大多数工程问题.

## 一、输入特性

**输入电流**: MOS 管栅极被氧化层隔离, 输入电阻大于 $10^{12}\ \Omega$, 输入电流小于 1 微安, 工程上视为 0.

**输入电平阈值**(以 74HC 系列,$V_{DD} = 5\ \text{V}$ 为例):

| 参数 | 符号 | 典型值 |
| ---- | ---- | ------ |
| 输入高电平最低值 | $V_{IH(min)}$ | 约 3.5 V($0.7 V_{DD}$) |
| 输入低电平最高值 | $V_{IL(max)}$ | 约 1.5 V($0.3 V_{DD}$) |

**输入保护网络**: 实际器件在输入端集成两只钳位二极管(一只到 $V_{DD}$, 一只到地) 和一只串联电阻. 当输入超过 $V_{DD} + 0.7\ \text{V}$ 或低于 $-0.7\ \text{V}$ 时, 二极管导通把栅极电压钳住, 防止栅氧化层击穿.

**重要推论**: 若输入电流被限制在 1 mA 以内(靠串联电阻或外接限流电阻), 可以安全地输入超过电源范围的电压, 这一特性被用于**电平转换**.

## 二、输出特性

**输出高电平** $V_{OH}$: 空载时接近 $V_{DD}$, 带灌入负载时因 PMOS 导通电阻 $R_{ON}$ 产生压降而下降.

$$V_{OH} = V_{DD} - I_{OH} \cdot R_{ON(P)}$$

**输出低电平** $V_{OL}$: 空载时接近 0 V, 带拉出负载时上升.

$$V_{OL} = I_{OL} \cdot R_{ON(N)}$$

**输出电流**: 74HC 系列的最大输出电流约 ±4 mA(5 V 供电), 因此驱动 LED 时必须串联限流电阻, 且**灌电流方式(输出低电平点亮 LED) 比拉电流方式更可靠**.

## 三、噪声容限

$$V_{NH} = V_{OH(min)} - V_{IH(min)}$$

$$V_{NL} = V_{IL(max)} - V_{OL(max)}$$

以 $V_{DD} = 5\ \text{V}$ 的 74HC 为例:$V_{OH(min)} \approx 4.4\ \text{V}$,$V_{IH(min)} \approx 3.5\ \text{V}$, 故 $V_{NH} \approx 0.9\ \text{V}$; 低电平方向 $V_{NL} \approx 1.5 - 0.1 = 1.4\ \text{V}$. 整体远优于 TTL 的 0.4 V.

**电源电压与噪声容限成正比**:$V_{DD}$ 越高噪声容限越大, 但功耗与延迟也越大.

## 四、功耗

**静态功耗**:

$$P_S = I_{DD} \cdot V_{DD}$$

$I_{DD}$ 为静态漏电流, 常温下每门小于 1 微安, 因此静态功耗可忽略. 高温下漏电流急剧增大, 是 CMOS 电路高温失效的主要原因之一.

**动态功耗**(主要部分):

$$P_D = C_L \cdot V_{DD}^2 \cdot f$$

其中 $C_L$ 是等效负载电容(包括本级输出电容, 下级输入电容和布线电容),$f$ 是输出翻转频率.

**短路功耗**: 输入跳变期间两管同时导通产生的瞬态电流功耗, 约占总动态功耗的 10% 至 20%. 输入边沿越缓, 这部分功耗越大.

**总功耗**:$P = P_S + P_D$. 工作频率越高, CMOS 的动态功耗越接近甚至超过 TTL.

## 五、传输延迟

$$t_{pd} = \frac{t_{PHL} + t_{PLH}}{2}$$

主要来源:

1. 对负载电容 $C_L$ 的充放电时间, 约 $R_{ON} C_L$.  
2. 器件内部的寄生电容.  
3. 前级驱动能力不足时, 输入波形的上升/下降时间会被计入.  

**降低延迟的手段**: 提高 $V_{DD}$, 增大管子的宽长比, 减小负载电容.

**速度与功耗的折中**是 CMOS 设计的核心矛盾:$P_D \propto V_{DD}^2$, 而 $t_{pd} \propto \frac{1}{V_{DD}}$, 因此低压设计省电但慢.

## 六、使用注意事项

1. **输入端禁止悬空**: 悬空栅极易积累静电荷击穿氧化层, 且电平不确定会导致输出级两管同时导通而发热损坏. 所有不用的输入端必须接 $V_{DD}$, 接地或与使用端并联.  
2. **遵守"与门接高, 或门接低"的多余端处理原则**.  
3. **防止可控硅锁定(闩锁效应, Latch-up)**: CMOS 内部存在寄生双极型晶体管构成的可控硅结构, 若输入/输出引脚电压超出电源范围较大且注入电流过大, 会触发正反馈形成低阻通路, 导致器件烧毁. 防护措施是加钳位二极管, 限流电阻, 并保证电源先于信号上电.  
4. **静电防护(ESD)**: 栅氧化层极薄, 人体静电即可击穿. 操作时应佩戴防静电手环, 器件存放在防静电包装中.  
5. **电源去耦**: 每个芯片的电源引脚附近应接一只 0.1 微法的去耦电容, 抑制动态电流引起的电源波动.  
6. **多余门的处理**: 不用的门输入端应固定接确定电平, 不能悬空.  
7. **不同系列混用时的电平匹配**: 4000 系列(3 至 15 V) 与 74HC 系列(2 至 6 V) 混用时, 必须核对 $V_{OH}$/$V_{IH}$ 是否匹配; 74HCT 系列专为与 TTL 电平兼容而设计.  

## 七、常见故障与原因

| 现象 | 常见原因 |
| ---- | -------- |
| 芯片发热严重 | 输入端悬空, 输出短路, 触发闩锁效应 |
| 输出电平不正确 | 负载过重超过驱动能力, 电源未去耦 |
| 高频下工作异常 | 动态功耗导致温升, 传输延迟累积 |
| 上电即损坏 | ESD 击穿, 电源接反, 输入超范围 |

## 易错点

- "CMOS 输入电流为 0, 所以扇出无限大"是错的: 扇出受限于**负载电容**带来的速度下降, 工程上按 50 估算.  
- 认为 CMOS 一定比 TTL 省电也是错的: 在很高频率下动态功耗可能超过 TTL.  

## 相关笔记

- 前置:[CMOS反相器](./CMOS反相器.md)  
- 对比参考:[TTL门电路电气特性](./TTL门电路电气特性.md)  
- 特性参数的统一定义:[门电路概述](./门电路概述.md)  
